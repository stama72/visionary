# W2-16 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

**「Skip で置く」「Trait + CI の別ジョブで置く」の2案はタスク仕様の「前提」が持つ**(implementer が読む位置にある必要があるため)。ここに書くのは仕様に残らなかったものだけである。

- **3条件を1本の集約 assert にする**(「3条件すべてを満たす日が30日続いてはいない」1本)。**却下理由**: 条件ごとの変異が判別できない — M-1(生産回数を常に1以上に見せる)を当てても条件2・3 の違反日が残るので緑のままになり、**「核心」印のテストが変異で担保されない**(W2-14 のテスト10 が残した穴と同じ形)。**却下の代償**: 走行回数が 5 → 15 に増える(30日 × 10世帯なので機械時間は安い。CLAUDE.md)
- **`ScanThirtyDays` の結果をシードで memo 化して走行回数を 15 → 5 に戻す。** **却下理由**: 決定論なので結果は同じだが、テストに静的な共有状態を持ち込む。**代償はコストだけで、保護は失われない**
- **条件3 を品目ごとに見る**(パン・薪・ビールのそれぞれについて「全世帯で0」を違反とする)。**却下理由**: issue #173 の閉じる条件より強い。ビールは嗜好([GDD02b §1](../03-gdd/02b-consumption-and-household.md))なので、都市から消えることは飢餓とは別の事象である。**却下の代償**: **「ビールだけが都市から消える」は検出されない。** 嗜好が最初に死ぬのは正常な縮小の形でもあるので、見たくなったら [#41](https://github.com/stama72/visionary/issues/41) のメトリクス側で見る
- **条件1 を [GDD02 §8](../03-gdd/02-economy.md)-3 の強さ(各レシピごと)へ上げる。** **却下理由**: issue #173 の条件1 は都市全体の合計であり、閉じる条件を勝手に強くしない。**却下の代償**: **1職業だけが止まり続ける状態(W2-14 が実測した鍛冶の詰み)は、この検出器では緑である。**
  - **訂正(フェーズ2、レビュー1巡目)**: 却下理由には当初「そちらは `SmithNeverRunsOutOfToolsOverSixtyDays`(60日)が別に見ている」と書いていたが、**この受け皿は存在しない。** 当該テストが見るのは鍛冶の**工房在庫[工具] ≥ 1** だけで、生産回数を読まない。同テストの doc コメント自身が「鍛冶は day 7〜8 に原材料を切らして以後生産0が続く」「#148 の閉じる条件の1つ目(生産の復帰)は本テストでは満たさない」と書いている。**「1職業だけが止まり続ける」を見る機械は、現時点でどこにも無い。** 却下理由の本体(閉じる条件を勝手に強くしない)は独立に成立するので却下自体は動かないが、**代償は埋まっていない** — フェーズ3 で issue に落とすこと

## 開発者が決めた2件(フェーズ1、2026-09-22)

- **反転して置く**(Skip でも Trait でもなく)。仕様の「前提」に理由ごと書いた
- **条件2 の母数は都市内の約定だけ**(窓口からの輸入・窓口への輸出を含めない)。**代償**: issue #173 本文の「day 20 以降 0」は母数が違う可能性があり、**実測した基準値がその数字と一致しないことがある**(未確認)。一致しなくても検出器の誤りではない

## implementer の件数(フェーズ2)

- **止まって報告した件数: 0 件**(4回の起動すべてで0)
- **決めて報告した件数: 6 件**
  - 1回目(実装): 失敗メッセージが参照する正側メソッド名を、そのメソッドが存在しなくても仕様の対応表どおりに書いた / 正側の失敗メッセージに条件1・2 の集計値も載せた
  - 2回目(6.1〜6.3): 6.3 の3行の置き場所(#1 は `ScanThirtyDays`、#2・#3 は条件3 の反転側。正側には重複させない)/ `scan.Format(seed)` を添える対象を反転側3本と読んだ
  - 3回目(6.4): 0 件
  - 4回目(実測の転記): M-13・M-14 は全検出器共通なので `ScanThirtyDays` に1箇所書いて各メソッドは `<see cref>` で参照 / 6.1 の裏づけも `ScanThirtyDays` の既存 remarks へ追記

**仕様の停止点(「実測が全シードで違反0なら止まる」)には当たらなかった。**

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 1・実測 | I-b-1 | **条件2 の母数を「都市内の約定だけ」にした決定を守る assert が無い。** M-5(テスト側 `!=` → `==`)も M-3(`src` 側 `ExecuteImport` の `CounterpartyId` → 0)も緑のまま通ることを実測した | **いま足せる歯止めがデータ依存になるため。** 「内部の件数と窓口の件数が一致しない」形の assert は、素の実装で両者が偶然一致しない前提に乗る。**レビュー3巡目が指摘したばかりの「保証がデータに乗っている」形をもう一度作ることになる。** かつレビューの巡は上限に達しており、足しても誰も見ない。**代わりに「守られていない」ことを doc コメントに断定として書いた**(6.5「守られていないと確定したもの」)。狭い側に倒す判断([03-corrections](../process/03-corrections.md)) |
| 1 | I-b-4 の代償 | **「1職業だけが止まり続ける」を見る機械がどこにも無い**(受け皿とされていた `SmithNeverRunsOutOfToolsOverSixtyDays` は工房在庫[工具] ≥ 1 しか見ない) | 本タスクは #173 の閉じる条件を見る検出器であり、条件1 を GDD02 §8-3 の強さへ上げることはフェーズ1 が却下済み。**却下理由の本体(閉じる条件を勝手に強くしない)は独立に成立する。** 却下の代償が埋まっていないという事実は上の「却下した設計案」に訂正として残した |

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 探索(全象限) | **3件**(I-b-4 / I-b-1 / I-b-2。**I-a は0件**) | 2件 | 1件 |
| 2 | **網羅パス**(列挙) | **2件**(日付の ±1 が4本とも緑 / スケジューラのシードが結果に影響しない) | — | 2件 |
| 3 | 象限I のみ | **2件**(違反日ラベルが assert されていない / 6.2 の「±1 どちらでも赤」がデータ依存) | — | — |
| 4 | **開いていない** | — | — | — |

**2巡目を網羅パスに充てたので、探索は1・3巡の2巡。** [01-review](../process/01-review.md)「網羅パスは巡を1つ消費する。上限4も減衰も動かさない」に従い、**3巡で打ち切った**(4巡目に到達していないので `REVIEW-EXHAUSTED` ではない)。

**網羅パスの収穫**: `src/` の状態を読む経路を33件列挙し、**反転した検出器は数え落とし方向の取り違えを原理的に検出できない**という構造を出した(核心が「違反日が存在する」なので、母数を減らす取り違えは違反日を増やすだけ)。**仕様の「落ちるべき条件」表の誤りは、すべてこの構造に由来していた。**

## `mutator` の件数(フェーズ2)

- **当てた変異: 14 件**(仕様の M-1〜M-4 + フェーズ2 が追加した M-5〜M-14)/ **期待と食い違った数: 1 件**
- 食い違いは **M-3**(`ExecuteImport` の `CounterpartyId` → 0)。仕様の期待は「赤」だったが**緑**。**仕様が先に「緑でも検出器の壊れではない・止まらなくてよい」と裁定していた**ので止まっていない
- 結果はすべて doc コメントと仕様 6.5 に転記済み

## フェーズ3 へ — issue に落とすもの

1. **条件2 の母数を守る歯止めが無い**(上の「直さないと決めた指摘」1件目)。#173 を閉じる前に、絞り込みを書き換える改修が入るなら人が見るしかない。[#41](https://github.com/stama72/visionary/issues/41) のメトリクス側で「窓口からの購入が約定に占める割合」を持つなら、そこと突き合わせる形にできる
2. **「1職業だけが止まり続ける」を見る機械が無い**(上の2件目)。W2-14 が実測した鍛冶の詰みが、どの検出器でも緑である
3. **M-4(消費の減算を削る)が条件1 の反転側も全5シードで赤にした。** 予測外の巻き込みで、**原因は特定していない**。消費を止めると生産が止まらなくなるという関係そのものが、#30 / #174 の見立てに関わる可能性がある
4. **issue #173 本文の実測表は条件3 について再現しない。** #173 は「シード1・day 30 に全10戸が0」を記録したが、本実測ではシード1 は違反0日で、違反はシード7 の day 30 のみ。**原因は特定していない**(doc コメントに事実として記録済み)
