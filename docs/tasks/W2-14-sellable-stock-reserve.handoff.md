# W2-14 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **`TradeSettlement` に番人を置かない(呼び出し側の切り詰めだけで担保する)** — 採らない。段5b と `StoreChoice` の2経路を直しても、**将来 工房在庫を直接読む呼び出し側が生えたときに機械が止めない。** #148 の追随表が約定の減算を「最終的な担保」と呼んでいるのは、そこが在庫を動かす唯一の場所だからである。引数1つと既存テストの修正がその代償
- **不変条件テスト(60日走行で鍛冶の工房在庫が常に ≥ 1)だけで担保する** — 採らない。上と同じ理由に加え、**60日走行は踏まれた経路しか見ない** — 番人は踏まれた瞬間にその場で落ちる。**却下したのは「番人の代わりにすること」であって、この断定をテストとして置くこと自体ではない**(2026-09-21 の訂正でテスト11 がこの形になった。番人と併置する)
- **検出器の下限を「全品目で毎日1件以上」に広げる** — 採らない。**完売由来の 0 件**(食料など需要の強い品目で正当に起きる)で赤くなり、留保と無関係な理由で検出器が鳴る
- **検出器の下限を日数比率(例: 60日中58日)で置く** — 採らない。#148 の**訂正9 が明示的に反転させた向き**へ戻る。0 件の日は検出したい事象そのものである
- **ヘルパーに `Recipe` を渡す形** — 採らない。別世帯のレシピを渡す取り違えが型で防げない。`(definition, household, itemId)` なら職業から引くので混ざらない(#39 の職業付け替えにも自動で追随する)
- **留保量で設備と入力を合算する形** — 採らない。[GDD02c §1.3](../03-gdd/02c-price-and-budget.md) は排他の分岐で書いており、M0 に該当品目が無い。合算は**新しい決定**であって実装の裁量ではない

## implementer の件数(フェーズ2 — 1回目。`IMPL-BLOCKED` で停止した回)

- 止まって報告した件数: **1 件**
- 決めて報告した件数: **4 件**

### 止まって報告した内容(1件)

**テスト11 `SmithProductionRecoversOverSixtyDays` が5シードすべてで赤。原因は留保ではなく、1次産品(鉄鉱石・木炭)の供給が60日もたないこと。**

留保そのものは仕様どおり効いており、テスト1〜10・12 は緑。鍛冶の工具在庫は60日間一度も 0 にならず 4〜7 で安定する(#148 の紙の予測「留保1 + 閾在庫3 = 4」とおおむね一致)。しかし鍛冶は day 7〜8 で鉄鉱石・木炭を使い切り、以後 生産回数 0 が60日目まで続く。

### 切り分けの実測(シード1。フェーズ2 が追加で指示したもの)

| 測ったこと | 結果 |
| ---------- | ---- |
| day 8〜60 に `world.Market` へ鉄鉱石・木炭の売り注文が立った日数 | **鉄鉱石 0/53日・木炭 0/53日**(全日0件) |
| 同期間の鍛冶2戸の所持金 | ゼロではない(household2 は大半10・時折70〜590 / household3 は大半8〜68・時折884) |
| 同期間の鍛冶の帳簿 `Purchase` に鉄鉱石・木炭が現れた件数 | **0件** |

**供給側の断絶であって、需要側(資金不足・走査順)ではない。** 鍛冶は買いに行く以前に、市場に売り注文が無い。

master(留保なし)との比較:

| | master | 本ブランチ(留保あり) |
| - | ------ | ---------------------- |
| 工具在庫が最初に 0 になる日 | day 1 | **60日中一度も 0 にならない** |
| 生産回数が最初に 0 になる日 | day 2 | day 8 |
| 鉄鉱石が最初に 0 になる日 | day 8 | day 7 |
| 木炭が最初に 0 になる日 | day 8 | day 7 |

**原材料の枯渇は留保が作った経路ではない**(master でも day 8)。留保が変えたのは工具在庫が 0 にならないことと、それに伴い生産0 の発生が day 2 → day 8 に遅れたことだけである。

### フェーズ1 へ — ここから先が設計判断である理由

1. **テスト11 の断定は [#148](https://github.com/stama72/visionary/issues/148) の閉じる条件そのもの。** 窓を変える・断定を弱める・2戸のうち1戸でよいとする、はいずれもその再解釈である
2. **原材料の供給断絶を直すのは本タスクのスコープ外。** 値付け・購入判定側の校正に触れるので、#148 決定1 が却下した「[GDD02d §4.4](../03-gdd/02d-external-market-and-money.md) の校正表を動かすこと」に当たる
3. **タスク仕様そのものに欠陥がある(象限 I-b)。** 変異 **M-1 の期待「テスト10 と 11 の両方が赤」は成立し得ない** — テスト11 は変異を当てる前から全シード赤なので、留保の有無を区別できない。**テスト11 は #148 が実測した詰み(工具切れ)を見ていない。** 見ているのは1次産品の供給断絶という別の事象である。一方 **テスト10(工具の売り注文が毎日1件以上)は緑**で、こちらは留保が効いていることを実際に捉えている

### 決めて報告した内容(4件)

1. **「鍛冶」役の作り方** — `EconomySystemTestFixtures.BuildDefinition` が単一レシピしか差し込めないため、既存ファイルの慣習に倣い `Occupation.Miller` の枠に工具/自己参照パンのレシピを差し込んだ。`ReserveQuantity` の設備分岐は品目が `Item.Tools` かだけを見るので職業名は結果に影響しない
2. **既存テスト `SellerReferenceIsTakenBeforeTheSellableStockGate` の修正方法** — 留保導入で「0個から1個買って1個とも輸出できる」前提が崩れたため、開始時の工具在庫を留保量ちょうど(1)足し、最終の期待値を 0 → 1 に変更。「段1 が販売在庫0 の分岐を捕まえる」という主眼は変えていない
3. **`SellableStockTests` に境界の具体例テストを1件追加**(`SellableStockIsWorkshopInventoryMinusReserve`、0→0 / 1→0 / 2→1 / 5→4)。仕様「規則6」の具体例をそのままテスト化したもの
4. **`ProductionSystem.cs` の doc コメント訂正箇所** — 仕様が指す2か所(`:17-18` と `:53-54`)のうち、誤った記述が実在したのは1か所のみ。行番号の古さと判断し、クラス概要側は整合するよう加筆した

## implementer の件数(フェーズ2 — 2回目。仕様の訂正を受けて再開した回)

再開時の作業ツリーには1回目の実装が未コミットで残っており、**訂正後の仕様と一致していた**(implementer が突き合わせて確認)。実装上の変更は旧テスト11 の差し替え1点。

- 止まって報告した件数: **0 件**
- 決めて報告した件数: **1 件** — テスト11 の doc コメントに実測値そのものは書かず、初版が空洞だった経緯と #30 への引き継ぎのみを記載した(実測値の転記は後工程と仕様が定めているため)。記述範囲の選択であって設計判断ではない

レビュー指摘の修正・実測値の転記で追加起動した implementer は3回。いずれも 止まって報告 **0 件**。

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 2 | I-a の付随論点 | **テスト10 の空振り防止 (iii)「延べ件数60以上」は核心の論理的帰結でしかない**(核心「どの日も1件以上」が真なら必ず真)。順序を直すと死んだ断定になる | (iii) を核心の後ろへ移す修正だけを入れ、**断定そのものは残した。** テスト11 では (iii) は冗長ではない — **工房在庫が残ることと、工具の売り注文が立つことは別の事象**である。仕様(テスト表#12)が明示的に要求している断定を、テスト10 側の冗長性だけを理由に削るのは仕様の書き換えに当たる。テスト10 では冗長であることを doc コメントに残した |
| — | 実測の食い違い(M-3・M-6) | **赤の範囲が仕様の記述より広い。** M-3 で `SellerReferenceIsTakenBeforeTheSellableStockGate` が、M-6 で `SellableStockIsWorkshopInventoryMinusReserve(toolStock:0)` が想定外に赤 | **欠陥ではない。** どちらも**同じ事象を別角度で見ている既存テスト**が巻き込まれただけで、変異が見たい事象を捉えていることの補強である。仕様の副作用一覧が網羅的でなかっただけなので、実測どおり doc コメントへ転記した |

## フェーズ3 へ — issue に落とすもの

**テスト10 `ToolOffersNeverDisappearOverSixtyDays` の断定は、どの変異でも担保されていない。**

M-1(留保を消す)を当てても4シードで緑のままである。留保があると工房在庫1 → 販売在庫0 で売り注文が立たないが、**M-1 は留保を消すので在庫1 でも立つ** — M-1 はテスト10 の下限を**満たしやすくする**方向に働き、向きが逆になる。詳細はタスク仕様 §「M-1 がテスト10 を動かさない理由」。

したがって**将来テスト10 の下限を 0 件/日 に緩めても、M-1〜M-6 は実測どおりのまま・通常走行も緑のままで、誰も気付けない。** テスト10 を担保する変異の設計は #154 のスコープ外(新しい変異を1件足して `mutator` をもう一巡回すことになる)なので issue へ落とす。**テスト10 自体は「鍛冶の生産が完全に止まったこと」の検出器として意味を持つので外さない。**

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 通常 | **1** | 2 | 0 |
| 2 | 1巡目より狭い | **1** | 0 | 0 |
| 3 | さらに狭い(影響大の側を厳しく) | **0** | 0 | 0 |

**4巡目は回していない。** 3巡目が0件で、当てる角度も尽きたため(3巡目は断定の中身・境界・分岐順・決定論の4点に絞って当て、いずれも食い違いなし)。**残して issue に落とす指摘はない** — 上の「フェーズ3 へ」は実測から出たもので、レビューの残件ではない。

### 象限I の内訳(1巡ごと)

- **1巡目**: 変異 M-3・M-4 が「経路を通したこと」を測れない — 番人(`TradeSettlement` の検査)が断定より先に `InvalidOperationException` を投げるため、赤の理由が「断定が捉えた」か「番人が落とした」か区別できない。**初版のテスト11 と同じ型の欠陥**(象限 I-b)。→ **タスク仕様を訂正**(`703c3b3`)。複合変異にした
- **2巡目**: 検出器テスト10・11 で空振り防止の断定が核心より先に置かれ、**弱い断定が強い断定の失敗を横取りする**。→ **直した**(`0da66a4`)

## `mutator` の件数(フェーズ2)

- 当てた変異: **6 件**(M-1〜M-6。使い捨て worktree `.pipeline/mutation/154` で単独当て・毎回復元。測定対象 HEAD `0da66a4`)
- **期待と食い違った件数: 3 件**(M-1・M-3・M-6)
  - **M-1** は期待が**狭すぎる方向**に外れたので**タスク仕様を訂正**(`8e31033`)。テスト11 は全5シード赤で期待どおり
  - **M-3・M-6** は**広すぎる方向**なので直さず、実測どおり転記(理由は上の表)
- ビルドが通らなかった変異・当てる場所が2通りに読めた変異: **0 件**
