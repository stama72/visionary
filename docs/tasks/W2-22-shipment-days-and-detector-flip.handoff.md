# W2-22 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **条件1・2 にも反転側を残す**(人為的に悪い世界を組んで「1日以上止まる」を維持する) — **採らない。**「素の `WorldDefinition.M0` を30日回す」という検出器の題そのものが変わる。#210 決定ログ1 の実測では違反日0のシードしか残らないので、維持するには世界を悪く作るしかない。
  **却下で失う保護**: **数え過ぎ方向の取り違えを見る目**(W2-16 の M-1 = `ProductionRuns = Math.Max(1, runs)` が反転側を全5シードで赤にしていた)。正側ではこれが緑になる。**穴の実際の大きさは V-2 が測る** — `ProductionSystemTests` の `Assert.Equal(0, ...ProductionRuns)` 群が同じ変異を捕まえていれば、検出器が見なくても suite は見ている。
- **数え過ぎを留める assert を足す**(例「生産0 の世帯が1戸以上ある日が30日のうちにある」) — **採らない。** データに乗った留め具になる。#210 決定ログ1 の day 29 の生産停止世帯は 2〜4 戸だが、経済が良くなれば 0 になりうる値で、そのとき「退行」と同じ形の赤が出る。`ScanThirtyDays` の doc コメントが 6.2 の等値 assert について同じ警告を既に持っている。
  **却下で失う保護**: 上と同じ。**2案とも却下したので、条件1・2 の数え過ぎ方向は本タスクの後、検出器の外にしか無い。**
- **条件3 の構造的な留め具(M-8 = 世帯在庫と工房在庫の取り違え)を本タスクで塞ぐ** — **採らない。** #173 の持ち物として W2-17 が既に issue へ送っており、本タスクは #216(出荷日数と向きの反転)である。
  **却下で失う保護**: 条件3 は**いまも**「世帯在庫が全品目0」と「工房在庫が全品目0」を判別しない(2026-09-22 実測、`mutator`)。本タスクはこれを直さない。

## implementer の件数(フェーズ2)

implementer は5回起動した(実装 / 1巡目修正 / 2巡目修正 / 3巡目修正 / 変異結果の転記)。

- **止まって報告した件数: 0 件**
  - 仕様が「止まって報告する」と定めた5条件(条件3 の退行 / 手順の無い既存テストの失敗 / `LongRunFinishesWithinTheBudget` の60秒超 / 60日走査で条件を満たす組が無い / 違反日の残るシード)のいずれにも当たらなかった
- **決めて報告した件数: 4 件**
  - 実装: `UnaffordableNecessityCountsOnlyTheFundsShortfall` の資金不足ブロックを選び直す 60日走査のスキャナを自分で組んだ(走査は使い捨てで、実行後にテストファイルから削除済み)
  - 1巡目修正: 条件2 には条件1 の「向きが変わって見えなくなったもの」に相当する remarks ブロックが元から無かったので、同趣旨のブロックを新設した
  - 1巡目修正: 「空振り防止(6本)。この4本は…」の件数のねじれが3箇所にあり、指摘が対象メソッドを特定していなかったので3箇所とも揃えた
  - 3巡目修正: 指摘4の「あわせて」が指す箇所が、指摘文の行番号レンジの外にあった(条件2 メソッド自身の「日付の ±1」remarks)。引用文言の完全一致検索で特定した

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 1 | I-1 の一部 | 検出器自身の空洞化に**構造的な留め具**を足す(判定式を合成値で突く `[Theory]`。前例は同ファイルの `IsHoardedWhileEmpty`) | **仕様が「構造的な留め具は足さない」と決めている。** 却下した案2 が退けたのは「データに乗った留め具」なので、この別形は**却下理由の射程外**である — つまり退けた根拠が無い。だが足すかどうかはスコープの判断であり、**フェーズ1 の持ち物。** 穴の所在は `V-5` / `V-6` / `V-10` が実測し doc に残した。**issue へ落とす** |
| 2 | 網羅パスの列挙 | `E-n` 約17件のうち `V-7`〜`V-12` に採らなかったもの | **本タスクで測るのは「doc の主張が保護を過大に言う側へ転じうるもの」に限った**(仕様 追補 D)。除いたのは (a) 害の向きが安全側(doc が保護を過小に言う)もの (b) 既測で向きに依らないもの(M-13 / M-14 / R-1〜R-5)。**列挙そのものが網羅パスの成果なので、表ごと issue へ落とす** |
| 1 | 疑い-1 | `UnaffordableNecessityCountsOnlyTheFundsShortfall` の**嗜好**ブロックが、出荷日数 1 の下では別の機構で緑になっている可能性(doc は「実効価格144 > 基礎値ゲート108」と機構を特定している) | **#216 の Exit Criteria を脅かさない。** 仕様 5.1 は**落ちなかったブロック**の機構の測り直しを要求していない(落ちたのは資金不足ブロックだけ)。機構が変わっていれば doc の実測記述が古くなるが、assert の判別力の喪失は「嗜好が買えなくても必需の資金不足に数えない」の一点に留まる。**issue へ落とす** |
| 3 | 象限III 2件 | 「この6本はデータを1行も参照しない」の派生記述のズレ(`FinalDayIndex` / `HouseholdCount` は `world` を読むので W2-16 6.4 の「4本」とは出自が違う)/ 削除した反転側の M-2・M-11 の詳細が `ScanThirtyDays` の一行にしか残っていない | **レビュアー自身が「判断の基準として参照される記述ではない」として報告対象外に置いた。** 記録のため書き留める |

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 探索 | **4件**(I-a 1 / I-b 3) | 1件 | 1件 |
| 2 | **網羅パス**(検出器群の doc コメントが主張する「機械が守っている / 守っていない」の全記述を列挙) | **1件**(I-b-1) | — | 1件 + **列挙23件**(`E-n`) |
| 3 | 狭めた(1巡目の修正が足した散文そのもの) | **4件**(すべて I-b。**I-a は0件**) | — | 0件 |

**3巡で打ち切った**(上限4には達していない)。**3巡目は I-a が0件で、出た4件はすべて「1巡目の修正が足した散文」の訂正だった** — 散文を直すと次の巡がその散文を指摘する自己言及的な減衰に入っており、もう一巡は読み直しではなく文面の推敲になる。**残っていた事実問題(既測の鮮度切れ・向きが変わった軸の保護の有無)は読み直しでは決まらないので `mutator` に渡した。**

**2巡目を網羅パスに充てた理由**: 1巡目の象限I 4件のうち3件が同じ型だった(「機械が何を守っているか」の doc の主張が、向きの反転や基準値の変化で偽になっている / どちらとも読める)。**列挙可能な境界が見えたので、探索の巡を1つ削って網羅に充てた**([01-review](../process/01-review.md)「網羅パス」)。**この巡が `V-7`〜`V-12` を生んだ。**

## `mutator` の件数(フェーズ2)

- **当てた変異: 12 件**(V-1〜V-12)/ **期待と食い違った数: 0 件**
- 2026-09-25、対象コミット `2c25b7a`、使い捨て worktree で1件ずつ。素の `2c25b7a` は631件全緑(基準)
- **向きは12件とも期待どおりだったが、粒度が期待と違ったものが3件ある**(いずれも doc へ正確に転記済み)。**「期待どおり」と丸めると消えるので、PR 説明では粒度のほうを書くこと**:
  - **`V-3` は 3/5 シードでしか赤にならない**(seed 1・3・42 のみ。seed 2・7 は緑)。仕様の表は「数え落とし → 正側は**見る**」と書いたが、**その「見る」はデータに乗っている**
  - **`V-4` / `V-12` が落としたのは核心ではなく 6.2 の等値 assert。** 相手軸と日付軸は**守られている**が、**報告するのは核心ではない** — W2-16 の「母数を都市内だけにした決定を守る assert は無い」は正側では偽になった
  - **`V-9` が落としたのは核心ではなく空振り防止** `Count < 30`
- **フェーズ1 が handoff に残した問い(「却下で失う保護」の実際の大きさ)に答えが出た**: `V-2` の数え過ぎ変異は**条件1 の検出器は 5/5 緑で見ないが、suite は9本で捕まえる** — `ProductionSystemTests` の4本と、**予期外に `EveryOccupationKeepsAtLeastOneCarrierOverSixtyDays`(5シード)**。**却下2案で失った保護は、検出器の外に実在した**
- **`V-5` / `V-6` / `V-10` は3条件とも全件緑で、捕まえたテストは1本も無い。** 「検出器は自分自身の空洞化を見ない」が3本とも実測で確定した

## issue へ落とすもの(フェーズ3 が立てる)

1. **検出器自身の空洞化に構造的な留め具を足すか**(上表1件目)。`V-5` / `V-6` / `V-10` が3条件すべてで穴を実測済み。**データに乗らない形(判定式を合成値で突く `[Theory]`)が候補で、却下した案2 の射程外である**
2. **網羅パスの `E-n` 列挙(約17件)。** 2巡目のレビュー報告の表をそのまま貼る。`V-7`〜`V-12` は本タスクで実測済みなので除く
3. **`UnaffordableNecessityCountsOnlyTheFundsShortfall` の嗜好ブロックの機構の鮮度**(上表3件目)
4. **`V-3` が 3/5 シードであること。** 条件1 の「数え落とし方向を見る力」はデータに乗っており、seed 2・7 では母数を1戸に絞っても違反日が0のまま。**シードの組を変えるか、構造で留めるかの判断**
