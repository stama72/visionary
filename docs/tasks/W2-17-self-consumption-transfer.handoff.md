# W2-17 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **順1 生産の直後に移す** — 採らない。段4 の需要は**順2 の後**の世帯在庫を予想在庫として読む([TDD01 §3.3](../04-tdd/01-sim-core-and-m0.md) 段4)ので、「消費を終えたあとに目標在庫へ届く量」という水準は順2 の直前でしか書けない。**失われる保護**: 生産と自家消費が1つのシステムに収まり、`ProductionSystem` のテストだけで移動を突ける読みやすさ。**補った**: 移動量の式を `SelfConsumption` 静的クラスへ切り出し、パイプラインを走らせずに境界を突けるようにした(タスク仕様「0. 置き場所」)
- **当日の消費に足りる量だけ移す**(最小の変更) — 採らない。世帯在庫が毎日0に戻るので在庫圧力‰ が 1500 に張り付き、**パン屋が都市でいちばん高くパンを買う世帯になる**。**失われる保護**: 移動量が最小で、市場に出る量がほとんど減らない。**代償として引き受けたもの**: 生産者は買い手より1日ぶん多く在庫を抱え、そのぶん販売在庫が減る(冬の木材加工で薪8個 / 日産36個)
- **消費の前に目標在庫へ届く量を移す** — 採らない。予想在庫が `目標在庫 − 1日消費量` になり、在庫圧力‰ が 1166 まで上がってパン屋が**相場の1.17倍まで払って他のパン屋からパンを買う**(代入で確認: 在庫比‰ = CeilDiv(1000×4, 6) = 667、圧力 = 1500 − CeilDiv(667,2) = 1166)。**失われる保護**: 式に `+ 今日の消費量` が現れないぶん単純で、`Lookahead` 1本で書けた
- **必需だけを対象にし、嗜好(ビール)を除く** — 採らない。用途で分岐すると「なぜ用途で違うのか」を GDD02b §1.1 が答える必要が生じ、「足すのは移動だけである」という節の性格が崩れる。**失われる保護**: 「嗜好は余剰資金で買う」という規律([GDD02b §3.1](../03-gdd/02b-consumption-and-household.md))が、**醸造のビールにだけ効かなくなる** — 醸造は困窮してもビールを諦めない。困窮の検出(§4)を醸造だけ鈍らせる方向である
- **必需の取り置きから自家供給できる品目を外すことを同じタスクに含める** — 採らない(開発者の決定、2026-09-22)。#174 の閉じる条件に不要であり、混ぜると「自家消費を入れて30日回す」の実測が2つの変更の合成になる。**失われる保護**: パン屋はパン3日分(≈324)、木材加工は薪7日分(≈280、冬は倍)の現金を凍らせ続けるので、**#173 が赤のままだった場合に主因の切り分けがもう1往復増える。** [#185](https://github.com/stama72/visionary/issues/185) が引き取る
- **自家消費を独立したシステム(順1.5)として足す** — 採らない。新しい `RandomStream` が1本要り([TDD01 §3.1](../04-tdd/01-sim-core-and-m0.md))、パイプラインの順の値が動く。**失われる保護**: 移動がパイプラインの表に1行として現れる可視性。**補った**: [TDD01 §3.3](../04-tdd/01-sim-core-and-m0.md) の順2 の行に移動を明記した

## implementer の件数(フェーズ2)

**6回起動。止まって報告した件数: 2 件 / 決めて報告した件数: 5 件。**

止まった内容(1行ずつ):

- **テスト表 #1 の「必需3日・嗜好2日を両方持つ品目」が構成不能。** `WorldDefinition` のコンストラクタが両方正を `ArgumentException` で弾く。**合成の定義でも作れない** → 象限I-b としてフェーズ2 が仕様を訂正した(`dd930ec`)
- **テスト #7 の「左項(世帯在庫が0だった観測)が正であること」を assert できない。** seed 42 では自家供給6戸すべてが30日間一度も世帯在庫0まで下がらない(最小値: h3 item7=1 / h4 item6=2 / h5 item7=1 / h6 item5=20 / h7 item5=16 / h8 item6=6)→ 留め方を「観測を全件記録し、抱え込みはその上の絞り込みとして導出する」形へ変える裁定を出して渡し直した(`5b04d8c`)

決めて報告した5件のうち、記録に値するもの:

- **初回実装で既存の統合テスト3件を再測定・更新した。** `UnaffordableNecessityCountsOnlyTheFundsShortfall`(世帯Id 4→6、day 7→8)/ `NecessityIsSettledBeforePreference`(世帯Id 0=醸造 は自家消費でビールを自給するため対照に使えず 6=製粉 へ差し替え、観察日数 3→5)/ `ProductionAndConsumptionRunInPipelineOrder`(「その日の生産分は同日消費の対象にならない」は自家消費が**意図的に崩す**不変条件なので期待値を更新)

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 1 | II | 仕様「3. 順序・境界」の**季節境界の例**(秋の最終日・木材加工、目標在庫 `4 + 8×6 = 52`)に対応するテストが無い | 名指しされた故障(`Lookahead` に渡す日数の取り違え)は**テスト #2 が春のパン屋で捕まえる**(目標在庫 6→4)。季節をまたぐ先読みの正しさは `DailyConsumption.Lookahead` 自身のテストが持ち、自家消費は季節固有のロジックを足していない |
| 1 | 参考 | GDD02b §1.1 の「**目標日数が 0 なら目標在庫も今日の消費量も 0**」は一般には偽(目標日数0でも消費量が正なら移る) | **`src/` の doc コメントは M0 の射程へ狭めた**(`3df283f`)。**GDD 側は触れない — フェーズ2 が触ると `SPEC-OUTSIDE`。** M0 では小麦粉・工具とも消費表の行が0 なので値は変わらず、テスト #9 が退行を捕まえる → **フェーズ3 が issue へ** |
| 2 | I-a | **条件3(全戸空)の数え過ぎ方向を見る検出器が消えた。** 正側の核心 `Count == 0` も空振り防止 `Count < 30` も違反日が**増える**方向しか見ない | **条件3 は #173(W2-16)の持ち物**で、#174 の閉じる条件は #7 である。#173 は条件1・2 が反転側のままなので閉じる段階になく、いま留め具を足すのは他 issue のスコープを #174 に取り込むことになる。**事実は `mutator` が確定させた(M-8 = 緑)**、doc コメントは訂正済み → **フェーズ3 が issue へ** |

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1(探索) | 実装・テスト・doc コメント | **3件**(I-a 2 / I-b 1) | 1件 | 0件 |
| 2(**網羅パス**) | `ScanThirtyDays` と4検出器が依拠する経路 | **3件**(I-a 2 / I-b 1)+ **37経路の変異表** | — | 1件(M-8 の赤/緑) |
| 3(探索) | 1・2巡目の修正が持ち込んだ欠陥 | **1件**(I-a)+ 参考1件(I-b-2) | — | 0件 |

**2巡目を網羅パスに充てた理由**: 1巡目の I-a・I-b がいずれも「**30日走査の検出器が空振りしたまま緑で通る経路**」に集まり、境界が列挙可能だった。**巡は1つ進むので、探索は1巡目と3巡目の2回**である。

**3巡で閉じた理由**(上限4に達する前):

- 3巡目の象限I は1件で、その修正は**留め具の追加のみ(実行されるロジックは不変)**
- **修正の効果は `mutator` の R-1〜R-4 が直接実測する設計**にしてあり、4巡目のレビューより実測のほうが強い証拠になる
- 残る指摘(上の表の2件)は**すべて issue へ落とす方針が決まっており**、巡を重ねても減衰しない

## `mutator` の件数(フェーズ2)

- **当てた変異: 12件 / 期待と食い違った数: 0 件**(実測日 2026-09-22、対象コミット `13144cf`)
- 内訳: 仕様の核心 M-1〜M-7(7件)/ 条件3 の M-8(1件・**赤緑は未知のまま測った**)/ この PR が足した留め具の検証 R-1〜R-4(4件)
- **M-7(`TakeOwnOutputHome` の呼び出し削除)は全5シードで「核心」で落ちた** — 空振り防止4本(`FinalDayIndex == 30` / `SelfSuppliableHouseholdCount == 6` / 観測件数180 / 最終日の突き合わせ)はすべて通過している。#7 に求めた判別力が核心にあることの実測
- **M-8 は緑のまま。** 詳細と扱いは上の「直さないと決めた指摘」2巡目 I-a を参照

## フェーズ3 への申し送り

- **#173 は閉じない。** 条件3(全戸空)は全5シードが正側へ移ったが、**条件1(生産停止)と条件2(都市内約定の消滅)は5シードとも反転側のまま**である。タスク仕様「作るもの 4」手順6 の「3条件すべてが正側になったら #173 が閉じる」には達していない。**issue #174「なぜ実装を先に置くか」が想定した「入れても赤のままなら主因は取り置きか天井」のほうに出た** — 自家消費は主因ではなかった
- **切り出す issue は3本**(上の2表が理由を持つ): (1) 条件3 の数え過ぎ方向の構造的な留め具(#173 に紐づく)/ (2) GDD02b §1.1 の「目標日数が0なら消費量も0」の一般化された言い方 / (3) 季節境界のテスト(任意)
