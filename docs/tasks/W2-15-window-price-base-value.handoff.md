# W2-15 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **`NoPurchaseReason.MarketTerm` を改名する** — 採らない。決定11 で分岐1 の材料は基礎値になったので、enum の名前は相場項が無い日の分岐も指すようになった。それでも改名しないのは、(1) [#120](https://github.com/stama72/visionary/issues/120) の追随表が「doc を直す」と決めていること、(2) [GDD02b §5.2](../03-gdd/02b-consumption-and-household.md) の3分岐の名前も「相場」のままであること、(3) `TradeSystem` の破産中フラグの判定と既存テストに波及し、[ADR-0008](../adr/0008-review-scope-narrowed-to-unnoticeable-defects.md) の「Exit Criteria を脅かすか」で仕分けると脅かさないこと、の3つによる。**失う保護**: enum の名前だけを読む人は「相場基準が無い日には立たない理由」と読む。**`PurchaseDecision.cs:13` の doc 1行がそれを止める唯一の手段になる** — だから仕様の追随表でその1行を明示的に要求している
- **窓口価格を `WorldDefinition.WindowPrice(itemId, season)` として公開する** — 採らない。呼び出し側が `BuyerDemand` 1つしかなく、[#172](https://github.com/stama72/visionary/issues/172) が移動平均へ差し替える場所そのものである。公開すると `ExternalSellPrice`(窓口が**売る**値。都市生産品では天井)と名前の近い読み口が `WorldDefinition` に3つ並び、取り違えの面が増える。**失う保護**: 単体テストで直接叩けない。テスト3 が `BuyerDemand.Build` 経由で**配線ごと**見ることで代える(規則7 と同じ向き)
- **検出器を1本にまとめる** — 採らない。#120 の全般アドバイザー指摘【3】のとおり、決定11 は購入量を1つも変えないので輸入額の走行テストでは M-2 が落ちない。**変異 M-2 の期待に「テスト1 は緑のまま」を書き込んで、この主張自体を実測させている**

## フェーズ1 が凍結前に直したもの

- PR **#175** をマージして GDD の決定10・11・12 を master へ入れた(`7377df3`)。**未マージのままだと implementer が読む GDD と仕様が食い違う**([#154](https://github.com/stama72/visionary/issues/154) と同じ形)
- GDD の誤り2種を直した(`efd8a3a`)。決定11 の取り残し4か所(うち GDD02b §3.2 は破産中フラグの経路)と、「零点の一致は恒等式」という過大な断定。**後者は #170 の本文にも同じ言い方がある** — 正確な形と M0 で到達しないことの実測は仕様の「前提」節にある

## フェーズ2 の再開時の状態(1回目の走行が `NO-SENTINEL` で止まった)

**1回目のパイプライン(`.pipeline/170-impl-20260922-173014.jsonl`)は `NO-SENTINEL` で停止した。** 枠の問題ではない(停止時点の 5時間枠の利用率 10%)。

| 事実 | 出所 |
| ---- | ---- |
| implementer(Sonnet)は起動し、**39k トークン出力・$2.81 ぶん働いた**。その途中で `[Request interrupted by user for tool use]` を受けて落ちた(`subagent_stats.failed: 1`。**開発者の中断ではない**) | 生ログの `result` 行 |
| **フェーズ2 のメインは「implementer は起動前に中断された。何も実装されていない。作業ツリーは clean」と報告した。これは偽である** — `git status` は 7 ファイルの変更を出す。メインは状態を確認せずに書いた | 生ログ最終メッセージ / `git status` |
| フェーズ2 は `PIPELINE: DONE` も `HALT` も出さず、**開発者へ質問して**ターンを閉じた | 同 |

**2回目の走行は、1回目の未コミットの実装の上から再開する**(本体ツリーは clean ではない)。フェーズ1 が機械で測った状態:

- `dotnet build Visionary.sln -c Release` — **成功・警告0**
- `dotnet test Visionary.sln -c Release` — **6件赤 / 432件緑 / 438件**。6件はすべてタスク仕様「既存テストの追随」の**訂正節**が裁定した族である
- 未コミット: `BuyerBudget.cs` / `BuyerDemand.cs` / `ErrandPlanner.cs` / `PurchaseDecision.cs` / `BuyerBudgetTests.cs` / `BuyerDemandTests.cs` / `TradeSystemTests.cs`

**捨てずに引き継ぐ。** 作り直しても同じ6件に当たる(根は仕様の穴であって実装の誤りではない)。

## implementer の件数(フェーズ2・2回目の走行)

**止まって報告 6件 / 決めて報告 1件**(2ラウンド)。

### ラウンド1(止まって報告 5件 / 決めて報告 1件)

前任の未コミット実装を引き継ぎ、未着手だったテスト1(`TradePipelineTests.WindowImportsOnTheFirstDayStayBelowATenthOfTheCitysMoney`)とテスト3(`BuyerDemandTests.BaseValueFallsBackToTheWindowPriceOfTheDay`)を追加した(コミット `e021288`)。

止まって報告した5件:

1. `TradeSystemTests.BudgetGateUsesTheEffectivePriceOnly` — 裁定表どおり、対称性を戻す前に原意の確認を求めた
2. `TradeSystemTests.NextDaysProductionDropsByTheErrandLaborLoss` — 梃子(窓口価格を上げる)の適用前の確認
3. `TradeSystemTests.ExportErrandIsSkippedWhenTheDayIsFull` — 同上
4. `TradePipelineTests.NecessityIsSettledBeforePreference` — 帯(100 vs 100,000)が崩壊。置き直しの可否
5. **`TradePipelineTests.UnaffordableNecessityCountsOnlyTheFundsShortfall` の第2ブロック(仕様の裁定表に無い、族の未列挙のメンバー)** — (世帯,日)の選び直しは手順どおり済ませた後、同じテストメソッド内の別ブロックが新たに赤になった

決めて報告した1件:

- `TradeSystemTests.NecessityIsSettledBeforePreference` の期待値 3→4。先行3 Assert が通っていることを確認のうえ特定した機構は、後日(2026-09-22、訂正4赤F)誤りと判明した ── 動いたのは木材(生産の入力、この世界ではCashCap=0で新旧とも購入0のまま)ではなく耐久(工具)の行(基礎値が現金上限15→窓口価格1に落ち、代金2→1)。原意「必需が先に決済される」は保たれる。**オーケストレータが妥当と判定した**

### ラウンド2(止まって報告 1件)

オーケストレータが5件を裁定して返し、1・2・4 は緑になった(コミット `1ef5ed2`)。

- 1 — 偽の観測(パン999)を home にも入れて対称性を回復。**確認済み**: home は引き続き窓口ではなく区画内の Miller(`CounterpartyId=1`)から床10 で買う(数量2・単価10)。足場の役割は保たれている
- 2 — 穀物の床 1→30。**確認済み**: 窓口(距離2)と売り手区画(距離2)が同点になるが、`ErrandPlanner.Plan` は区画Id昇順・厳密な `>` 更新なので売り手が勝つ。窓口は候補に入り直していない
- 4 — 帯を置き直した。**実測**(M0・シード1・世帯Id0・3日で流動資金を段階的に振った): 1〜70 は薪の約定が成立しビールは0件、72 以降はビールも成立。境界は 70〜72 の間。安全側に寄せて `ScarceLiquidFunds` を 100→**50**。`AmpleLiquidFunds`(100,000)と観察日数(3日)は境界の外なので動かしていない

**3 は梃子が効かないことが判明して未修正のまま報告された。** これが下の停止の直接の原因である。

## 停止(`PIPELINE: HALT IMPL-BLOCKED`)— 未解決の赤2件【2回目の走行。**3回目で解決済み**】

**停止時の機械の状態**(オーケストレータが自分で測った、2026-09-22):

- `dotnet build Visionary.sln -c Release` — **成功・警告0**
- `dotnet test Visionary.sln -c Release` — **2件赤 / 442件緑 / 444件**
- `dotnet format Visionary.sln --verify-no-changes --severity warn` — **通過**
- 作業ツリー clean(`e021288` / `1ef5ed2` の2コミット済み)

**2件とも根は基礎値の族である**(族の外の赤は1件も出ていない ⇒ 決定10・11 の実装そのものに欠陥は見つかっていない)。**止めたのは、仕様の裁定表が挙げた梃子が効かず、代わりの配置を選ぶことが設計判断になるからである。**

### 赤1 — `TradeSystemTests.ExportErrandIsSkippedWhenTheDayIsFull`(`TradeSystemTests.cs:651`)

**裁定表が名指しした梃子(窓口価格 = 基礎値を上げる)が、このテストの caseB では構造的に効かない。**

- caseA(売り手が距離2)は赤2 と同じ理由で安全 — 窓口も距離2 で同点になり、区画Id昇順で売り手が勝つ
- **caseB(売り手が距離4)では窓口が必ず勝つ。** `ErrandPlanner.EstimateWindowPrice` は(距離 > R・記憶が無い日は)未知価格の床を使い、売り手側の `TryEstimateOfferPrice` も同じ床を使う。**両者の見積もり価格は常に同額**になるので、**床をどんな値に上げても距離だけで決まり、距離2 の窓口(district 4)が距離4 の売り手(district 8)に必ず勝つ**(実測: `plan.VisitedDistrictIds = [4]`)
- **このテストの判別軸は往復時間そのもの**(「日が埋まっているとき輸出の外出が飛ばされる」)なので、窓口が勝って往復4時間に縮むと軸が消える

**フェーズ1 への論点**: 代わりの配置には少なくとも次があるが、どれも「このテストが何を測っているか」を読まないと選べない — (a) 距離4 の売り手に有効な価格記憶を仕込んで見積もりを窓口より下げる(テスト1 の足場と同じ手)、(b) 窓口の区画を遠くに置く、(c) caseB を分離する。**オーケストレータは仕様の外の設計判断と判定して選ばなかった。**

### 赤2 — `TradePipelineTests.UnaffordableNecessityCountsOnlyTheFundsShortfall` の第2ブロック(`TradePipelineTests.cs:996`)

**仕様の裁定表(族6件)に挙がっていないメンバーである。** (世帯,日)の選び直し(世帯Id6・8日目 → **世帯Id4・7日目**)は確立手順どおり済んでおり、そちらは緑。赤なのは同じテストメソッド内の別ブロック(素の M0・day1・世帯Id1 がビールを買わないことの断定)である。

**implementer が旧コード(`7377df3`)との突き合わせで裏付けた実測:**

- **旧コード**: 世帯Id1(Baker・流動資金 2400)の基礎値が現金上限(600/1200/2400/400/400)になり、在庫圧力も掛からないので段5a で**一日で 2,388 を使い切る買い占め**(薪32・パン8・工具1・小麦粉2・薪4)が起き、Preference の番には流動資金 12 しか残らず、価格72 のビールを**買えなかった**
- **新コード**: 同じ行の基礎値が窓口の当日価格(10/54/290/56/10)に落ち、day0 で予想在庫 ≈ 目標在庫 のため必需・耐久・入力がほぼ買われず、**流動資金 2400 が Preference まで温存されてビールが買える**

**期待を反転するだけでは直らない。** `Assert.False(boughtBeer)` を `Assert.True` にすると `Assert.Equal(0, ...)` は通るが**自明化する** — 何も失敗していない世帯で「資金不足カウントが0」を示すだけになり、**「嗜好の購入失敗は資金不足に数えない」という、このブロックが証明したかったことを何一つ証明しなくなる**(implementer の診断)。

**フェーズ1 への論点**: 「嗜好の購入が資金不足**以外**の理由で失敗する」世帯・日を新しい規則のもとで作り直す必要がある。**どの理由で失敗させるかの選択が設計判断である。**

### 仕様への追随(フェーズ1 が引き取る)

**タスク仕様の「### 訂正(2026-09-22、フェーズ1)」は族を7件と書いたが、実際は8件である**(赤2 が未列挙)。**あわせて赤1 の裁定行(「望ましい梃子は窓口価格を上げること」)は caseB では成り立たない。** オーケストレータはタスク仕様を書き換えていない(フェーズ2 は仕様を書き換えない)。

---

# フェーズ2・3回目の走行(完了)

**2回目が `IMPL-BLOCKED` で残した赤2件は、フェーズ1 が仕様に「訂正2」を書いて裁定済みだった**(`b55eefc`)。3回目はそこから再開し、**レビュー3巡と `mutator` 10件を経て緑で完了した。**

## implementer の件数(3回目の走行)

**止まって報告 0件 / 決めて報告 3件**(4ラウンド)。**2回目の「止まって 6件」から 0 に落ちた** — 差は仕様の側にある。訂正2 以降、裁定が「採る梃子・確かめて報告すること・不成立時の落とし所」の3点セットで書かれるようになり、implementer が判断を要する余地が消えた。

| ラウンド | 渡したもの | 止まって | 決めて | 決めて報告した内容 |
| -------- | ---------- | -------- | ------ | ------------------ |
| 1 | 訂正2 の赤1・赤2 | 0 | 0 | — |
| 2 | 訂正3(レビュー1巡目の象限I 3件) | 0 | 1 | 訂正2 が書いた `BudgetGateUsesTheEffectivePriceOnly` ↔ `ExportErrandIsSkippedWhenTheDayIsFull` の相互参照が、赤A の梃子変更で事実と食い違ったので併せて直した |
| 3 | 訂正4(レビュー2巡目の象限I 4件) | 0 | 1 | **訂正4 赤E の指示文が home について広すぎることを実装が見抜き、従わずに home / distant を場合分けして書いた。** レビュー3巡目が実装側を正と確認し、オーケストレータが仕様を狭めた(`bbcde4c`) |
| 4 | レビュー3巡目の象限I-b 1件 + `mutator` の実測の転記 | 0 | 1 | 無効化の一文の文言を、同じ doc が既に持つ打ち消しの書式に合わせて組み立てた |

**ラウンド3 の「決めて報告」は先行指標として重い。** 仕様(= オーケストレータの訂正)の誤りを実装側が検出して差し戻した唯一の例である。

## レビューの巡ごとの件数(3回目の走行)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 探索(決定論・窓口価格の振り分け・季節・既存テストの判別力・規則8 の表) | **3件**(I-a 2 / I-b 1) | 0件 | 0件 |
| 2 | **網羅パス**(配置を変えたテストの全件列挙と感度判定) | **4件**(I-a 1 / I-b 3) | — | 0件 |
| 3 | 前巡4件の閉じ確認に限定 | **1件**(I-b) | — | 0件 |

**3巡で打ち切った**(上限4に達していない)。理由は2つ。**(1)** 2巡目を[網羅パス](../process/01-review.md)に充てて境界を閉じた — 配置を変えたテストは**既存10件 + 新規4件 = 14件**で全件列挙・感度判定済みであり、探索をもう1巡回しても同じ欠陥類の取りこぼし確率は下がらない。**(2)** 3巡目の指摘は doc の配置1件のみで、その修正後にコードの面は1行も動いていない。残る検証価値は読み直しではなく実測の側にあり、そちらは `mutator` 10件で尽くした。

### 網羅パスが効いたことの証拠

**1巡目・2巡目の象限I のうち3件(赤A・赤C・I-a-2)は「テストは緑だが核心の断定が飽和・自明化している」という同一の欠陥類である。** `dotnet test` はこの類を1件も報告しない。特に:

- **赤A** — 訂正1 が名指しした梃子(偽の観測を home にも入れる)が、両辺を上側 clamp(2T)に飽和させて `Assert.Equal(home, distant)` を「2 == 2」にしていた。**#154 の型を警告している当の指示で再現した**
- **赤C** — 帯 100→50 の置き直しが走査順の判別力を消していた。`mutator`(M-6)が確定させた

## 直さないと決めた指摘(3回目の走行)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 2 | I-a | `TradePipelineTests.NecessityIsSettledBeforePreference` が**パイプライン級で走査順を検出しない** | **帯の置き直しでは復元できない。** ビールの床が 72 で、70以下では嗜好が絶対額で塞がれ、72以上では買える — 「必需は払えるが嗜好は走査順のせいで買えない」帯がこの世界に存在しない。復元には M0 の価格か世界の構成を変えることになり**本タスクの外**である。**走査順の規則そのものは `TradeSystemTests` の3件が守っている**(M-6 で実測)ので [M0 の Exit Criteria を脅かさない](../adr/0008-review-scope-narrowed-to-unnoticeable-defects.md)。**消さずに、持っていない保証を doc 冒頭で名指しで宣言した**(訂正4 赤C の裁定1・2) |

## フェーズ3 が issue へ落とすもの

1. **パイプライン級の走査順の検出器を建て直すか。** 上の「直さないと決めた指摘」。判断には M0 の価格(特に `ExternalBuyPrice[Beer] = 72`)か世界の構成を動かすことが要る。**`TradePipelineTests.NecessityIsSettledBeforePreference` の doc 冒頭に、持っていない保証と引き取り先が書いてある**
2. **`BuyerBudget.BaseValue` のガードが、決定10 の反転を検出器より手前で捕まえる。** M-1 の実測で表面化した(下記)。害ではないが、**次に M-1 を当てる人は同じ期待で詰まる**

## `mutator` の件数(3回目の走行)

- **当てた変異: 11件**(M-1〜M-10 を1件ずつ + 裁定のために2巡目の途中で当てた M-6 の診断1回)
- **期待と食い違った件数: 0件**
- 途中の1回(M-6 の診断)は**転記用ではなく裁定用**である。赤C の「帯の置き直しでは復元できない」を決めるのに実測が要った。**テストが動いた後に測り直しており**、doc コメントに載っているのは測り直したほうである

### M-1 で表面化した非対称(仕様の期待と実測が違った唯一の点)

**仕様は「M-1 の失敗メッセージから day0 の輸入額を読む」と書いていたが、読めない。** 決定10 を反転すると `cashCap = 0` の行が生じ、**本タスクで足した `BaseValue` の戻り値ガードが `ArgumentOutOfRangeException` を投げて、検出器の断定に到達する前に落とす**(赤 76件)。**2つの機械が別の面を守っていることの現れであり、欠陥ではない。** テスト1 の doc コメントに転記済み。輸入額が実測できたのは M-3 のほう(5752 / 4568 / 4568 / 4744 / 4408、いずれも閾値 2400 超)。

## 実測して doc コメントへ転記したもの

**仕様の「実測して doc コメントへ転記すること」2件とも転記済み。**

**1件目** — テスト1 の5シードの day0 輸入額(2026-09-22 実測・60日走行後):

| シード | 1 | 2 | 3 | 7 | 42 |
| ------ | --- | --- | --- | --- | --- |
| day0 の輸入額 | 816 | 816 | 816 | 816 | 776 |

**#120 の使い捨て実測(816 / 816 / 776、シード 1/2/42、決定11 を入れる前)と完全に一致する。** 増えていない。初日は誰も相場基準を持たないので決定11 は初日には効かず、**初日の値を決めているのは決定10 だけである。** 閾値 `10 × 816 = 8,160 < 24,000` で通る。

**2件目** — **M-2(決定11 の反転)でテスト1 は全5シード緑のまま。** 赤になったのはテスト2 の1件だけ(`Reason` が `MarketTerm` → `None`、購入量は 0 のまま)。**「決定11 は購入量を1つも変えない」の実測であり、検出器を2本に割った根拠そのものである。**
