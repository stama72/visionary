# W2-11 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **帯の定数を品目ごとに置く(工具 2 倍・パン 12 倍 のように)** — 採らない。頭打ちを外す変異に対する判別力は 1 定数(床 × 20)で足りる(素の master はビールが 20 日目に 21.6 倍)。
  **代償: 帯の定数はパンの完売枝のラチェット(9.26 倍)で決まっており、他 4 品目には緩い。** 工具(1.17〜1.53 倍)が 10 倍になっても緑のまま通る。品目別の帯は #38 の後の実測([#120](https://github.com/stama72/visionary/issues/120) の残り)で置くかを決める。
- **`NecessityIsSettledBeforePreference` の定数(資金 100 / 100,000・3 日)を新しい実測(1,000 でも 1 日目にビールが約定する)に合わせて狭める** — 採らない。[#81](https://github.com/stama72/visionary/issues/81) の検出器の再校正は判別力の再実測を伴う契約変更で、W2-09 が「止まるべきだった」と数え直した型そのもの。
  **代償: remarks の実測値と定数の余裕が乖離する**(100,000 は必要以上に潤沢)。判別力そのものは変異 M-4 で測り直す — **M-4 が緑のままなら、据え置いた前提が崩れている。**
- **`ObservationBecomesUsableOnTheNextDayNotToday` の修正で、帳簿の行を直接置く代わりに実際の買い手を置く** — 採らない。買い手を置くと在庫が減って在庫比が動き、テストの主張(観測が翌日使える)と無関係な量が期待値に混ざる。
  **代償: フィクスチャが `LedgerEntry` の形(`Sale` / `ItemId` / `OccurredAt`)を直接知る。** `TradeSettlement` が帳簿の書き方を変えても、このテストの前提は動かず、`TryPreviousDaySettledPrice` が読む欄とずれても緑のまま通る。押さえているのは `PreviousDaySettledPriceIsQuantityWeighted` の 1 件だけ。
- **頭打ちの判定を `OfferPrice` の中で帳簿から求める(`Calculate` に `IReadOnlyList<LedgerEntry>` を渡す)** — 採らない。`OfferPrice` は純関数で `World` を受け取らない(W2-08 の切り出しの理由)。段1 が既に `hasSettled` を持っている。
  **代償: 「錨」と「頭打ち」が同じ母数であることは、段1 が同じ 1 つの bool を 2 か所に渡していることだけで保たれる。** テスト #5 が配線を踏むが、2 つ目の呼び出し側が現れたときに片方だけ別の判定を書いても機械は止めない。

## パイプラインの経過(1 回目は `NO-SENTINEL` で停止、2 回目で完了)

**1 回目のフェーズ2 は `NO-SENTINEL` で止まった**(2026-09-20 21:18 頃。生ログ `.pipeline/131-impl-20260920-204506.jsonl`)。原因はレビュー2巡目のレビュアーが **600 秒無進捗で watchdog に落とされた**こと(直前に 5 時間枠の利用率 82% の `rate_limit_event`)。実装と 1 巡目は済んでいたので、**2 回目のフェーズ2 は implementer を最初から起動せず、2巡目から続けた。**

| 項目 | 状態 |
| ---- | ---- |
| 実装 | `f3e7243`(1 回目) |
| レビュー1巡目の修正 | `3206b0a`(1 回目。3 件すべて「直す」) |
| レビュー2巡目(網羅パス) | **2 回目でやり直して完了。** 43 件を列挙し I-b 3 件 → `309db2c`(仕様の訂正)・`81f8b3c` |
| レビュー3巡目 | **2 回目で実施。I-a 0 件**、I-b 2 件 → `0faf06f`(仕様の訂正)・`87d4837`。**ここで閉じた**(4巡目に到達していないので `REVIEW-EXHAUSTED` ではない) |
| `mutator`(M-1〜**M-5**) | **2 回目で実施。食い違い 0 件。** 転記は `8e5d56f` |
| build / test / format | 2 回目のフェーズ2 が各コミットで自分で確認した(最終 `8e5d56f`: 警告 0 / 365 件緑 / 差分なし) |
| 作業ツリー | 1 回目のレビュアーが `dotnet new console -o .probe` で本体に残した `.probe/` はフェーズ1 が削除済み。`mutator` の使い捨て worktree も削除済み。clean |

**`mutator` の M-5 は、レビュー2巡目の網羅パスの列挙からフェーズ2 が立てたものである**(タスク仕様の別表)。仕様の変異表は M-1〜M-4 までしか持っていなかった。

## implementer の件数(フェーズ2)

### 1 回目(実装)

- 止まって報告した件数: **0 件**
- 決めて報告した件数: **3 件**(いずれもテストの配置場所。契約に触るものは無い — フェーズ1 の事後の仕分け)
  - `OfferPriceTests` の新規 4 件を `OfferPriceFloorIsExternalBuyPrice` と `BankruptSellerFixesCoefficientAtFivehundred` の間に置く
  - `UnsoldSellerIsCappedAtTheReferenceInThePipeline` を `BankruptSellerPostsTheHalvedFloorInThePipeline` の直後に置く
  - `OfferPricesStayWithinTheBandOverSixtyDays` を `KnowledgeSpreadsBeyondTheHomeVisionRadius` と `ObservationsDoNotGrowWithoutBound` の間に置く
- 仕様の実測値との食い違い 1 件(仕様の欠陥。止まる/決めるには数えない): 仕様「設計の前提」のシード 3 の最大は「工具 444(1.53 倍)」ではなく **パン 195(3.61 倍、11 日目)**。工具 444 は 15 日目以降パンが市場から消えた後の定常値で、フェーズ1 が最終日近傍の値を最大と取り違えた。帯の定数 20 は動かない。implementer が remarks に正しい値を書いた

### 2 回目(レビュー2・3巡目の修正と、変異の転記。3 回起動した)

- 止まって報告した件数: **0 件**
- 決めて報告した件数: **1 件** — 変異の実測を XML doc コメントに写すとき、失敗メッセージの `<=` を `&lt;=` にエスケープした(数値と文言は変えていない)。テストプロジェクトは `GenerateDocumentationFile` が無効でエスケープ無しでも警告 0 で通るが、正しい XML として残す判断

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |

**無し。** 1〜3巡目の象限I はすべて「直す」で処理した。**issue へ落とした指摘も無い。**

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 探索(全体) | **I-b 3**(帯の定数の根拠に赤側 21.6 倍が無い / 帯の検出器の summary と失敗メッセージが「§1.1 が帯を保つ」と断言 / `NecessityIsSettledBeforePreference` の「判別力は維持されている」が頭打ち前の実測であることが読めない)。I-a 0。すべて「直す」→ `3206b0a` | 0 | 0 |
| 2 | **網羅パス**(触った doc コメント / remarks の実測・保証・範囲限定・参照の主張を全件列挙)。**1 回目は watchdog で落ちたので 2 回目がやり直した** | **43 件を列挙し、I-b 3**。I-a 0。すべて「直す」→ `309db2c` + `81f8b3c`。うち **1 件はタスク仕様そのものの欠陥**で、フェーズ2 が仕様を訂正した(`309db2c`) | 0(表には残した) | 0 |
| 3 | 探索(訂正の 2 コミットが主対象) | **I-a 0**。I-b 2(訂正が書いた因果が逆 / 完了条件が M-4 止まりで M-5 が測られないまま `DONE` になりうる)。両方「直す」→ `0faf06f` + `87d4837` | — | 0 |

**3巡で閉じた。** 3巡目の I-a が 0 件で、残る指摘も無い(4巡目に到達していないので `REVIEW-EXHAUSTED` ではない)。

**網羅パスの境界の引き方(次のタスクへの実測)**: 1巡目の I-b 3 件が**すべて同じ型**(文言が、測っていないことを断言している)だったので、2巡目を「触った doc コメント / remarks の主張を全件列挙し、各件の裏付けを (a) この差分のテストが機械で見ている / (b) 実測の出所が読める / (c) 机上の断定 に分類せよ」の形で渡した。**43 件中 3 件が (c) 側の I-b**。探索を 1 巡削った代償は、3巡目の I-a が 0 件だったことで回収できたと見てよい。

### フェーズ2 が仕様を訂正した 2 件(象限 I-b。詳細はタスク仕様の別表とコミット本文)

| 巡 | 何が仕様の欠陥だったか |
| -- | ---------------------- |
| 2 | §4 の remarks 指示「約定が無い売り手は相場基準が立っても**床より上へ**出ない」が偽。反例は仕様自身のテスト表 #5(床 10・相場基準 200 の売れていない売り手が 200 を提示する)。この文を規則として読むと「§1.1 に水準の復元力がある」となり、GDD02c §1.2 の囲みと正反対になる。**GDD / TDD は動かない**(§1.2 の囲みは元から正しい)ので `SPEC-OUTSIDE` ではない |
| 3 | 完了条件が「変異 M-1〜M-4」のままで、2巡目で足した M-5 が入っていなかった。このままだと M-5 を測らずに完了条件が満たされ、2巡目の是正(テスト表 #7 の判別力を現本体で測り直す)が実行されないまま `DONE` が出る |

## `mutator` の件数(フェーズ2)

- 当てた変異: **5 件**(M-1〜M-4 は仕様の変異表、**M-5 は 2巡目の網羅パスの列挙からフェーズ2 が立てた**)/ **期待と食い違った数: 0 件**
- 測った対象: `87d4837`(2026-09-20)。コールドベースライン 365 件全緑。5 件ともビルドは通った
- **据え置いた前提は 2 件とも生きていた** — M-4(`demand.Lines.Reverse()`)は赤で、#81 の検出器は頭打ち導入後も判別力を保っている(仕様は「緑のままなら象限 I-a」と書いていた)。M-5 も赤で、構成を差し替えた後の `ObservationBecomesUsableOnTheNextDayNotToday` にも判別力がある
- **M-5 の当て方には制約があった**(フェーズ3 が PR に書くなら添えること): 段6 は段5a が確定する `visitedDistrictIds` に依存するため、段1 より前へ移すには**空配列を渡す**必要がある。訪問区画に基づく他のテストが巻き添えで赤になるのはこの副作用で、対象テストの判別力の証拠ではない
