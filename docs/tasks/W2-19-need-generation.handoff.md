# W2-19 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **`Need.Id` を内容から導く**(`Id = ((世帯Id × 理由数) + 理由) × 品目数 + 品目`)— 状態もカウンタも要らず、`World.Needs` の削除・並べ替えに**構造的に**強い。採らないのは、**失効した組が後日また立ったときに同じ Id が返る**からである。M0 には `Promise` を作る処理が無いので今は害が出ないが、順6 を足した日に「失効した Need を指していた約束が、別の Need を静かに指す」が成立する。**この却下で失う保護**: 状態が1欄(`World.NextNeedId`)増え、ハッシュの入力・初期化・保存の経路がそのぶん増える。**添字参照に戻る事故を防いでいるのはテスト13・14 だけである**
- **遠方在庫を順4 で判定し直す**(`Ledgers` から「前日その品目を買ったか」を読み、目標在庫と予想在庫を順4 で解き直す)— `HouseholdState` に欄を増やさずに済む。採らないのは、**目標在庫と予想在庫の式が `BuyerDemand` と2か所になる**からである([02-task-spec](../process/02-task-spec.md) 規則5。`Recipe.CapacityRuns` が同じ理由で1か所に寄せてある)。**この却下で失う保護**: 順5 と順4 の間に「順5 が毎日正しく書いている」という暗黙の契約ができる。**契約を守るのはテスト20〜24 だけで、型も例外も守らない**(`int[]` の添字と単位の取り違えは黙って通る)
- **困窮を破産中フラグだけで立てる**([GDD02b §4](../03-gdd/02b-consumption-and-household.md)「判定の規範は §3.3 の破産中フラグ」をそのまま読む)— 引用に最も忠実で、条件が1つで済む。採らないのは、**フラグが在庫を見ないと §3.3 自身が明記している**からで、Need(= 不足の表面化)としては広すぎる。**この却下で失う保護**: 今日から食えなくなった世帯の困窮 Need が1日遅れる(フラグが前日ベースのため)。**[GDD02 §8](../03-gdd/02-economy.md)-2 (a) を「困窮 Need の件数」で代替できなくなった** — (a) が測るのはフラグそのものである(GDD02b §8.1 に書いた)
- **`LaborCapacity` を切り出さず、順4 で設備係数と労働力合計‰を綴り直す** — `ProductionSystem` に一切触らずに済み、差分が小さい。採らないのは規則5(同上)。**この却下で失う保護**: `ProductionSystem.RunOneHousehold` を書き換えるので、既存の `ProductionSystemTests` が守る範囲の外(構成員の走査順・`max(0, …)`)に手が入る。**見張りはテスト25 だけである**

## implementer の件数(フェーズ2)

implementer は7回起動した(初回の実装 + レビュー由来の修正5回 + 実測の転記1回)。

- **止まって報告した件数: 1 件**
  - 3巡目の網羅パス由来の指示「#29・#30 のアサートを配列全体へ広げる」では、変異 A23(走査後の 0 戻しループの初期値を `itemId = 1` にする)を殺せない。#29 は薪(添字5)を対象にしており品目 Id 0 を踏まず、#30 の穀物は終始「買えない」構成なので `boughtItem[0]` が真にならない。**「そのテストの他の主張は変えないこと」という私の指示の範囲を超えるため、自分で構成を変えずに報告した**(判断は妥当。凍結済み仕様の穴ではなく、私が出した3巡目の指示の穴だったので `IMPL-BLOCKED` には当てず、別テスト #33 を足す形で裁いた)
- **決めて報告した件数: 13 件。** 内訳は初回6・1巡目修正2・2巡目修正2・3巡目修正2・#33 の構成1。**大半はコミット本文と doc コメントに残るので、ここには残らないものだけ書く**:
  - `IntegerMath.FloorPermille` の戻り値型を `long` のままにした(`ApplyPermille` は `int` を返すが、`Recipe.CapacityRuns` はこの値をさらに除算するため、早期に `checked((int)…)` するとオーバーフローの発生タイミングが変わりうる。「`CapacityRuns` の挙動は一切変えない」を厳密に守るための判断)
  - `World.NextNeedId` が `internal set` でテストプロジェクトから直接書けないため、`HashChangesWhenNextNeedIdChanges` は `NeedGenerationSystem` を実際に走らせて間接的に値を動かす形にした
  - `SyntheticLoadSystem`(Runner 側。別アセンブリなので `internal set` に書けない)は `Need.Id` に `world.Needs.Count` を使う合成値へ変更した
  - #33 は穀物(品目 Id 0)を1次産品にできない(`BuildDefinition` の埋めレシピが必ず品目 Id 0 を出力する)ため、相場観測を1件仕込んで需要側の基礎値を実際の窓口価格に一致させた
  - **1巡目の初回報告に誤りが1件あり、後で訂正された**(「#20 の末尾に `Assert.Equal` を1行加えた」→ 実際は編集中の重複挿入を直後に除去しており正味の差分ゼロ)

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 2・3 | I-a | テスト #24 の変異(行単位の `if (purchased) { …; continue; }` ガードを外す)が赤にならない | 1巡目の修正で入った走査後の品目単位 0 戻しに**完全に包含されて観測不能**になった(ガードを外しても結果が変わらない)。挙動そのものは #29・#33 が守る。**ガードは防御として残し、`mutator` には渡さなかった。** タスク仕様の別表にその旨を明記してある |
| 3 | — | 網羅パス A10(足し込みの条件 `ExpectedStock < TargetStock` を `<=` にする)を測っていない | **等価変異である**(差が 0 のとき 0 を足すだけ)。この条件を測るなら A9(ガードごと削除)であり、それは #21 が既に殺す |
| 3 | — | 網羅パス A8(段5b が**全世帯**について毎日走ることを守るテストが1件も無い) | 世帯ループは本タスクが触らない既存コードで、穴を持ち込んだのは本タスクではない。**フェーズ3 で issue へ落とす** |
| 3 | — | 網羅パス A2 / A3 / B13(`UnfilledPurchase` の配列長と `StateHasher` の書き込み位置を守る**参照ベクトル**が無い。絶対ハッシュを固定したテストが1件も無い) | 同上。`StateHasher` 全体に及ぶ既存の構造的な穴であり、同一ビルド内の A/B 比較には効かない。**フェーズ3 で issue へ落とす** |

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 探索(全体) | **3 件**(すべて I-b) | 0 件 | 0 件 |
| 2 | 探索(1巡目の修正) | **1 件**(I-a) | 0 件 | 0 件 |
| 3 | **網羅パス**(`UnfilledPurchase` の書き込み・読み出しの全経路) | **0 件** | — | 0 件 |

**3巡で打ち切った。** 1・2巡目の象限I が**どちらも同じ面**(`UnfilledPurchase` の書き込みと読み出し)に落ちたので、3巡目は探索をやめて[網羅パス](../process/01-review.md)に充てた。列挙された41経路のうち「どのテストも守っていない」6種を洗い出し、値のある4種(A16 / A7 / A23 / B7)をすべて塞いだ。**3巡目の象限I が0件で、残る緑は上表の「直さないと決めた」4件だけになったため、4巡目は回していない。**

**網羅パスの副産物として、いちばん効いたのは A16 である** — 遠方在庫の数量を決める唯一の式から `− ExpectedStock` を落としても全テストが緑だった(既存の #22・#23 が**どちらも `ExpectedStock = 0` の世界**で書かれており、#21 はガードに弾かれて式へ到達しないため)。#31 がこれを塞いだ。

## `mutator` の件数(フェーズ2)

- **当てた変異: 8 件 / 期待と食い違った数: 0 件**
- 対象は `7cb44e6`、使い捨て worktree は `.pipeline/mutation/40`(片付け済み)。ベースライン532件全緑
- 8件のうち4件はタスク仕様の元の表の「核心」(#5・#12・#23 と、#20 から移った #30)、4件はレビューで足した別表の核心(#29・#31・#33)と網羅パス由来の A7
- **測った結果は各テストの doc コメントに転記済み**(出所を `mutator` と明記)。**内訳表はコミット `8240565` と doc コメントが持つので、ここには残さない**
