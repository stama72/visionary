# W2-13 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 却下した設計案(フェーズ1)

- **`externalSellPriceBase[都市生産品] == 0` の検査を外す** — issue [#149](https://github.com/stama72/visionary/issues/149) の表 #3 の文言(「1次産品についてだけの検査に変える」)を素直に読むとこうなる。採らない理由は、**外した瞬間に表へ置いた値が黙って無視される**こと。都市生産品の外部売値は外部買値からの導出が正(GDD02d §3)なので、基準値の表に独立値を置ける形は二重定義になる。同じファイルの季節係数行が「都市生産品の行はすべて 1000(使わないが、黙って効く値を置かせない)」で同じ穴を塞いでおり、そちらと非対称にする理由が無い。**検査は残し、例外メッセージだけを現行の規則へ直す**(仕様 決定1)
- **`ExternalMarket.TryOfferPrice` を `int OfferPrice(...)` へ単純化する** — M0 では常に `true` を返すので `Try` の形が嘘になる、という読みは正しい。採らない理由は2つ。(1) 呼び出し側 `StoreChoice.cs:112` / `ErrandPlanner.cs:276` に波及し、issue が「変更不要で天井が働く」と書いた最小の差分から離れる。(2) [GDD11](../03-gdd/11-external-trade.md) が窓口を行商人へ置き換えると「その日は並ばない」が戻るので、`Try` の形は先で要る。**代わりに「M0 では常に true」を doc コメントで固定する**(仕様 決定2)
- **帯の検出器が上限を `definition.ExternalSellPrice(itemId, 季節)` から読む** — 導出と一致して読みやすいが、**交易マージン‰ を動かすと検出器の閾値が一緒に動く**。天井の値そのものを疑う変異(M-2)に対して無力になるので、リテラル `2` を検出器が自分で持つ(仕様 テスト #3)
- **帯の検出器の判定対象を `world.Market`(提示価格)のままにして閾値だけ 20 → 2 にする** — [GDD02 §8](../03-gdd/02-economy.md)-5 は提示価格が天井を上回ることを許しているので、仕様どおりの経済でも赤になる。**判定対象を約定価格(`world.Ledgers`)へ移すことが #120 の閉じる条件そのものである**

## フェーズ1 の裁定(2026-09-21。`IMPL-BLOCKED` から復帰)

**停止は正しい。報告1 の赤は実装ではなく仕様の誤りだった。** 仕様側を訂正したので、**作業ツリーの実装は破棄しない**(`src/` 4ファイル・`tests/` 9ファイルの未コミットの変更はそのまま使う)。

- **報告1(帯の検出器が5シード赤)** — 仕様の欠陥(象限I-b)。初版のテスト #1 は「都市生産品の約定すべてが ×2 以下」と書いていたが、[GDD02c §1](../03-gdd/02c-price-and-budget.md) の囲みと [GDD02d §2.2](../03-gdd/02d-external-market-and-money.md) の囲みは**天井が区画ごとに柔らかいこと**を決定として置いている(「周縁の売り手は、窓口へ行く手間のぶんだけ外部売値を上回っても売れる」)。issue の閉じる条件も「**継続して**上回らない」である。**断定を 1-a(構造: 中心区画の買い手)と 1-b(水準: 全区画・実測で置く定数)に分けた。** オーケストレータの未検証の仮説(b)は当たっていた — ただし直す先は検出器であって GDD ではない
- **報告2(`MarketReference.TrySeller` が窓口の観測を畳む)** — **現行仕様どおりであり、変えない**(仕様 決定5)。[GDD02c §1.2](../03-gdd/02c-price-and-budget.md) の材料は「他の売り手ごとに最新の1件」で、窓口は [GDD02d §2.1](../03-gdd/02d-external-market-and-money.md) が売り手 Id を予約した売り手である。**doc コメントで経路の活性化だけを記録する**
- **(a) 帯を緩める / (c) 窓口の到達範囲を広げる は採らない。** どちらも GDD の書き換えであり、GDD は既にこの状態を正しいと書いている。**書き換える必要が無かった**
- **残り10件の赤** — 期待値の置き直しを許可した(仕様の「窓口が都市生産品を並べたことで期待値が変わる既存テスト」)。**判別力が吸収されるものだけ止まって報告する**

## implementer の件数(フェーズ2)

- 止まって報告した件数: **2 件**
- 決めて報告した件数: **6 件**

### 止まって報告した内容

1. **【核心】`TradePipelineTests.SettledPricesStayWithinTheBandOverSixtyDays` が5シードすべてで赤。** 仕様どおりに実装した検出器(約定価格 ≤ `ExternalBuyPrice × 2`)が、M0・60日で都市生産品の約定価格の天井超えを拾う
2. **`Observations.CollectWindow` の拡張(作るもの4)が、凍結済みの `MarketReference.TrySeller` の挙動を変える。** `TrySeller` は `Knowledge` の観測を `SellerId` で畳んで「他の売り手」として相場基準の平均に混ぜ、売り手Idの種別を見ない。本タスク前は窓口が1次産品の観測しか生まなかったのでこの経路は構造的に不活性だったが、窓口が都市生産品を並べた結果、中心区画の売り手が窓口の観測を自分の錨に取り込むようになった。仕様の「変えない既存コード」表は `TryPreviousDaySettledPrice` が窓口の**約定**を数える規則を挙げているが、窓口の**観測**を `TrySeller` が数えるこの経路は表に無い

## フェーズ2 の実測(オーケストレータが確認)

`dotnet build -c Release` 警告0 / `dotnet format --verify-no-changes --severity warn` 差分なし / `dotnet test -c Release` **15 赤・393 緑・計 408**。

赤のうち検出器の5件は、超過がいずれも小さい:

| シード | 品目 | 約定価格 | 床 | 帯の上限 | 超過 |
| ------ | ---- | -------- | -- | -------- | ---- |
| 1 | パン | 112 | 54 | 108 | +3.7% |
| 2 | パン | 114 | 54 | 108 | +5.6% |
| 3 | 薪 | 22 | 10 | 20 | +10% |
| 7 | パン | 109 | 54 | 108 | +0.9% |
| 42 | パン | 116 | 54 | 108 | +7.4% |

**天井は概ね効いている。** #120 の実測でシード2 は28日目に**床の20倍**を超えていたが、天井を入れた後の最大は床の **2.11倍**(114/54)。帯は 20× → 約 2.1× に潰れており、残っているのは検出器の断定(`≤ ×2`)と実測の数%の食い違いである。**この表は「最初に見つかった違反」までの値で、超過の上限は未測定**(検出器が最初の違反で止まるため)。

残り10件の赤は、窓口が都市生産品を並べたことで「1回の外出で複数品目を同時に買える」構造が生まれ、テストの厳密な境界値(余剰 = 費用のちょうど等号など)と絡んだもの(`ErrandPlannerTests.SecondErrandIsMeasuredAgainstTheDistrictsAlreadyChosen` ほか)。implementer は、配置や無関係品目の床を動かすだけで数値を変えずに直せた10件は直し、元の意図を壊さずには直せない残りを赤のまま報告した。

## 停止時の作業ツリーの状態

**コミットしていない。** `feat/149-external-sell-price-ceiling` の作業ツリーに `src/` 4ファイル・`tests/` 9ファイルの変更が未コミットで載っている(`MarketReference.cs` と `docs/03-gdd/` `docs/04-tdd/` `docs/adr/` は未変更)。test が赤のままコミットすると完了条件を満たさない状態を隠すため。**フェーズ1 が裁定するまで、このツリーを別の作業で上書きしないこと。**

## フェーズ1 へ返す論点

報告1 を直す道は3つあり、**どれも `docs/` に届く**(= フェーズ2 の権限外):

- (a) 帯の上限を緩める → [GDD02d §3・§4.4](../03-gdd/02d-external-market-and-money.md) の「帯 = [床, 床×2]」の書き換え
- (b) 検出器を**中心への到達可否で限定する** → 天井が縛る範囲の定義。[GDD02d §3](../03-gdd/02d-external-market-and-money.md) の囲みと [GDD06 §3.1](../03-gdd/06-trade-and-negotiation.md) に届く
- (c) 窓口の到達範囲を広げる → [GDD02d §2.1](../03-gdd/02d-external-market-and-money.md) の変更

**(b) についての未検証の仮説**(オーケストレータは `src/` を読んでいない): 天井は clamp ではなく店選択から創発するので、**縛るのは中心に届く買い手だけ**である。`IsWithinReach` が偽の区画には窓口という選択肢が無く、都市内の売り手が天井超えで約定できる。テスト #1 は到達可否の限定なしに「都市生産品の行すべて」と書いており、構造上満たせない可能性がある。**フェーズ1 は、違反した約定の買い手が中心に届いていたかを確かめるところから始めるとよい。**

報告2 は、塞ぐ先が `MarketReference`(本タスクのスコープ外)か GDD02c であり、同じくフェーズ1 の裁定が要る。なお**提示価格が天井を超えること自体は [GDD02 §8](../03-gdd/02-economy.md)-5 が許している**ので、報告2 単独では赤の説明にならない — 約定まで届くには「窓口を選べない買い手」が要る。

## 直さないと決めた指摘(フェーズ2)

なし(レビューに到達せず)。

## 巡ごとの件数(フェーズ2)

レビュアーは起動していない。build / test が緑でないため、レビューの前に停止した。

## `mutator` の件数(フェーズ2)

起動していない。M-1〜M-4 の実測は未実施(仕様どおり、レビューの巡が閉じた後に測る)。
