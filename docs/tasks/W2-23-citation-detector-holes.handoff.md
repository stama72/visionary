# W2-23 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 再開メモ(フェーズ1、2026-09-25)— 残っているのは実測と転記だけ

**1回目のフェーズ2 は `IMPL-BLOCKED` で止まった。原因はタスク仕様が名指しした許可の綴りで、フェーズ1 が直した**(仕様「核心の変異」の訂正 / `scripts/pipeline.ps1`)。**実装とレビュー3巡は完了して committed である**(`3dcdacd` 実装 / `c8f98c4` 1巡目の訂正 / `8c3fc54` 仕様の訂正 / `3fd2010` 別表 / `3d635f0` 本メモ)。**実装をやり直さない。**

**残っているのはこの3つだけである。**

1. **`mutator` に M-1〜M-11 を当てさせて実測する**(仕様「核心の変異」と「別表」。通る打ち方は仕様の表にある)
2. **期待と食い違った件数が 0 でなければ3択で裁き**、結果を implementer に**コミットメッセージへ転記させる**
3. **下の「未着手で残したもの」**(穴2 の既存本文「件数だけ数える」が新しい出力契約と食い違う)**を、その最後の implementer 巡に畳む。** 仕様 §4(b) は「既存の本文は変えない」と書いているが、**この1文だけは直す** — フェーズ1 の裁定である(象限I-b。凍らせた文面が出力契約と矛盾している側が誤り)

**この節はフェーズ3 が PR 説明へ転記したら消える。**

## 却下した設計案(フェーズ1)

- **検出器に自動テスト(fixture コーパス + ランナー)を新設する** — 採らない。fixture を `src/` `tests/` に置くと**検出器自身の走査対象に入って件数を汚す**。外へ置くには「走査対象を差し替える引数」を足すことになり、**CI 外の、手で叩く道具に契約の面を1つ増やす**。対象は awk 数十行である。**失う保護**: フェンス対応と一覧の回帰は、以後 `mutator` を明示的に回さない限り誰も測らない(今回の M-1〜M-3 が唯一の実測になる)
- **集計行に `RESOLVED <K>` を足して「0 件が保証する範囲」を自動で数えられるようにする** — 採らない。#117 が決めた契約の変更は**UNRESOLVED の一覧化だけ**で、集計行の形を変えると `2>&1 | tail -1` で集計を取る使い方が同時に動く。**失う保護**: 保証範囲の件数(1020)は手で数え直す必要が残る — **数え方のコマンドを冒頭コメント(4.(f))に書いて補った**
- **一覧で「引き継ぎ打ち切り」由来(`タスク仕様` / `ADR-` / `#<数字>`)に別のタグを付ける** — 採らない。#117 の前提どおり**一覧は人が追うための材料**であり、分類の誤りが機械の顔をして出るほうが高い。**失う保護**: 66 件を読む人は、GDD/TDD の節でないものを自分で見分ける必要がある
- **字下げされたフェンス記号もフェンスと見る**(CommonMark の 3 スペースまでを含む) — 採らない。**本物の見出しを飲み込むと STALE の偽陽性になる**。0 件を要求する検査に偽陽性が混じると「0 でなくてよい検査」に変わる(W2-09 の判断)。**失う保護**: 列0 以外で開いたフェンスの中に列0 の見出しがあると、いまも幻の節を作れる — **穴6 に残ると明記させた**

> **裸の `§` 48 件の書き換え**と**枝番 `-N` の照合**も採らないが、これは #117 決定1-1 と #118 決定1-2 が理由ごと持っている。issue に残るのでここには書かない。

## implementer の件数(フェーズ2)

- 止まって報告した件数: **0 件**
- 決めて報告した件数: **1 件**
  - 確認 #4 の `grep -rn 'GDD03 §1.2' src` が、仕様の期待 2 件に対して**実測 3 件**。差分は `GameDate.cs:26` の `<summary>1始まり(GDD03 §1.2)。…</summary>` で、**#233 の対象3件とは無関係な既存の正しい形式の引用**(`git show bc60efe:` で書き換え漏れでないことを確認済み)。仕様12行目「実測値を採って報告する」に従い実測値を採用

## 直さないと決めた指摘(フェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 2 | I-a の一部 | **状態がファイルをまたいで漏れないこと**を変異で測っていない | 測るには awk の起動をファイルごとからディレクトリごと1回へ**書き換える**必要がある。本タスクは C# を1行も変えない範囲で検出器を直すタスクであり、測定のためにスクリプトの構造を変えるのは変異ではなく改造である。現況では閉じないファイルが 0 件なので漏れても結果は動かない。**別表に「測らないと決めた経路」として理由つきで残した** |
| 2 | I-a の一部 | **見出しレベルの上限**(`#{2,6}` の 6)と**下限側の緩め**(`#{1,6}`)が一切測られていない(上限は 3 まで下げても通る) | **本タスクが新設した判定ではない**(W2-09 からの既存仕様)。別表に無検証として残した |
| 2 | I-a の一部 | **`README.md` の除外 guard が二重に死んでいる**(`${base%%-*}` が `README.md` をそのまま返すので doc_id は原理的に引用側と一致しない) | 同上。本タスクが新設した判定ではなく、現況では両 README に `^#{2,6} <番号>` が 0 件 |
| 3 | II/III(報告外) | 打ち切り語が現れても `last_doc` を破棄しないため、`… GDD0x §1 … タスク仕様 §8・§9` の3件目が GDD 側へ引き継がれる | **規則の文面どおりの挙動**(「直前の引用と当該 § のあいだ」)。該当行は現在 0 件で、踏めば偽 STALE として表に出るので気付ける |

**未着手で残したもの**(次のフェーズ2 が拾う): 冒頭コメント 穴2 の**既存本文に残る「件数だけ数える」**が、新しい出力契約と食い違っている(3巡目が象限III として報告)。仕様 §4(b) が「既存の本文は変えない」と凍らせているため、直すには仕様 (b) の訂正を伴う。**`mutator` の実測結果を転記する最後の implementer 巡に畳む予定だったが、下記のとおり実測に到達しなかった。**

## 巡ごとの件数(フェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 探索(全象限) | **1 件**(I-b-1 広い保証) | 1 件 | 0 件 |
| 2 | **網羅パス**(見出し集合を作る側の全判定) | **2 件**(I-a 1・I-b-1 1) | — | — |
| 3 | 探索(I-a のみ。引用走査側と UNRESOLVED 一覧) | **0 件** | — | **0 件** |

**網羅パスに充てたのは2巡目。** 境界は「**見出し集合を作る側の全判定**」— 1巡目の欠陥の出所がそこで、かつこの集合は**本変更の前後で完全に不変**(フェンスは全て列0、`~~~` 0 件、フェンス内の見出し行 0 件)なので、フェンス判定のどの条件を間違えても既存の検査は全て緑のまま通る。**探索は1巡目と3巡目の2巡**([01-review](../process/01-review.md)「代償は探索が1巡減ること」)。

**3巡で打ち切った理由**: 3巡目が象限I-a 0 件・疑い 0 件で、かつ `scripts/` `src/` は2巡目の対象(`8c3fc54`)から1バイトも変わっていない(`3fd2010` の差分はタスク仕様への別表追加のみ)。**4巡目に渡す対象が無い。** 象限I の残る指摘は 0 件なので、issue へ落とすものも無い。

## `mutator` の件数(フェーズ2)

- 当てた変異: **0 件 / 11 件** — **権限拒否で1件も実測できなかった。** 期待と食い違った数は**報告不能**(実測自体が行えていない)
- **拒否されたコマンド**: `bash .pipeline/mutation/233/scripts/check-doc-citations.sh`(タスク仕様が名指しした唯一の実行経路。redirect あり・なしの2形とも `Permission to use Bash has been denied`)
- 使い捨て worktree の作成(`git worktree add --detach .pipeline/mutation/233 3fd2010`)は成功しており、**`git` は通って `bash <script>` だけが弾かれている**。測定不能と判明した時点で worktree は削除済み(`git worktree list` にエントリなし)。**本体ツリーには fixture も変異も一切当てていない**
- **崩れた前提**: 仕様 §「含まない」の最終行が「`Bash(bash .pipeline/mutation:*)` は `scripts/pipeline.ps1` の許可一覧にフェーズ1 が足してある」を根拠にしていた。**綴りが間違っていた** — 照合はトークン境界で切れていないと当たらず、`bash .pipeline/mutation` は `/233/…` の途中で切れている
  - > **訂正(フェーズ1、2026-09-25)。** この節は当初**「許可一覧は `mutator` サブエージェントの `Bash` 権限には効いていない」**と書いていた。**誤りである** — 同じ一覧の `Bash(git:*)` で `git worktree add` は通っており、`implementer` も `Bash(bash scripts/check-doc-citations.sh:*)` で検出器を叩けている。**効いていなかったのは綴りのほうである。** パイプラインと同じ許可一覧で `claude -p` を起こして実測し(2026-09-25)、スクリプトのパス全体を綴った形では `permission_denials` が空になることを確かめた。**「機械がこう動いている」と書く前に壊して確かめる**([03-corrections](../process/03-corrections.md))に当たる誤りで、そのまま残すと**次に許可一覧を触る人が「サブエージェントには効かない」を前提に設計する**
- **迂回しなかった理由**: 許可済みの `Bash(bash scripts/check-doc-citations.sh:*)` は**本体のスクリプトしか許さない**。worktree に `cd` して同じ綴りで打てば通る可能性があるが、それは許可一覧が意図した対象とは別のファイルを実行させることになり、権限決定の回避にあたる。仕様自身も「**拒否されたら言い換えず報告すること**」と定めている
