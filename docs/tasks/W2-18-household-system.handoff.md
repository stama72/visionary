# W2-18 引き継ぎメモ

**この文書は PR 説明へ転記して削除する。**

## 1回目のフェーズ2 の停止と、フェーズ1 の裁定(2026-09-23)

**1回目のフェーズ2 は `IMPL-BLOCKED` で止まった**(実装本体と新規テスト #1〜#13 は緑。赤は既存の固定検出器3本・8インスタンスだけ)。**仕様どおりの停止である** — タスク仕様「赤の種類」表の第3区分(検出器の前提が崩れた)は「止まって報告する」と定めている。

**フェーズ1 が裁定し、タスク仕様に「5. 既存検出器の置き直し」(裁定 D-A・D-B・D-C)として書いた。** 実測で分かったのは、④が GDD02b §4.2 の規則どおりに発火し、**担い手 0 は一度も起きていない**一方で、**赤になった3本はいずれも「担い手 2 が続くこと」を暗黙の母集団にしていた**ことである(§4.2 は「防いでいるのは担い手 0 だけ」と明記している)。

**裁定の要点**: D-A は空振り防止の固定値 180 を構造的下限(毎日3戸以上)へ、D-B は核心と母集団がずれた空振り防止(延べ件数 ≥ 60)を落とす、D-C は「工具の売り注文が毎日1件以上」を「**5職業すべてに担い手が毎日1戸以上**」へ置き直す。**D-C は緩めた置き直しではない** — GDD02b §4.2 の「最後の1世帯は付け替えない」を、テストが初めて機械で守ることになる。

## 却下した設計案(フェーズ1)

- **④の付け替えを `WorldDefinition` のフラグで切れるようにする案**(`IsExportEnabled` に倣う) — 採らない。GDD に無い調整軸が1つ増え、M0 の実測が「④を切った経済」になる。[GDD02 §8](../03-gdd/02-economy.md)-3(生産チェーンが止まらないか)は職業分布が動くことで観測が豊かになる、と GDD02b §4.2 が明言している。
  - **失われる保護**: 既存の60日検出器(`SmithNeverRunsOutOfToolsOverSixtyDays` / `ToolOffersNeverDisappearOverSixtyDays` など)が④の発火で赤になったとき、「④のせいか / 他の変更のせいか」を切り分けるスイッチが無い。
  - **代替手段はある**: 使い捨て worktree で `FullPipeline` から順3 を外せば同じ比較が取れる。**本体ツリーには書かない**(ADR-0013)。

- **担い手世帯数を手順2 のループ前にスナップショットする案** — 採らない。同職業の2戸が同じ日に3条件を満たすと両方が離脱し、GDD02b §4.2 が「構造的に防ぐ」と宣言した担い手 0(薪の売り手が消え、パンとビールが同時に止まる)が起きる。
  - **失われる保護**: 付け替えの結果が世帯 Id の走査順に依存する。**先の世帯の付け替えが、後の世帯の候補と `argmin` を動かす。** これは GDD02b §3.2 が資金について既に受け入れている順序依存と同種で、ADR-0002 が要求するのは「順を Id 昇順に固定すること」までである。代わりに「両方が離脱しない」ことをテスト #10(核心 C-3)が押さえる。

- **④の連日発火を抑える冷却期間(「N日連続で困窮したら付け替える」)** — 採らない。GDD02b §4.1 が「閾値が1つ増える」として既に却下しており、その却下理由は本タスクでも崩れていない(3条件はいずれも既にある状態である)。
  - **失われる保護**: 付け替えが毎日・往復に起きうる(A → B → A)。付け替え直後の世帯は新職業の入力を持たないので生産量 0・販売在庫 0 になり、フラグが立ち続ける限りゲートが毎日開く。GDD02b §4.2 は「付け替えが頻発するなら価格機構が働いていない兆候である」と書いており、**頻発そのものは観測対象であって異常ではない**。④のゲートを別の状態へ移すかは [#196](https://github.com/stama72/visionary/issues/196) が持つ(本 issue の後)。

- **`#37` の申し送り(必需の予算そのものが 0 の世帯が件数に数えられない)を本タスクで直す案** — 直す対象が既に無い。#97 が「購入量 0 の理由」(GDD02b §5.2)の固定順を実装した結果、流動資金 0 の世帯は現金上限 0 で `NoPurchaseReason.CashCap` に落ち、経路(1)で数えられる。実効価格が第 1 項も超える日に `MarketTerm` が勝つのは GDD02b §3.2 の「高すぎて買わなかったは資金不足ではない」「同点も 1 が勝つ」どおりであり、逆転ではない。**判定は issue のコメントに残した。**
  - **失われる保護**: なし(仕様の側が変わったのではなく、#37 当時に無かった実装が入った)。

## implementer の件数(1回目のフェーズ2)

- 止まって報告した件数: **3 件**(いずれも `FullPipeline` に順3 を挿したことで既存の固定検出器が赤になった件。仕様の「赤の種類」表の第3区分「検出器の前提が崩れた」)
  - `TradePipelineTests.SmithNeverRunsOutOfToolsOverSixtyDays`(seed:7)— 60日間の工具の売り注文の延べ件数が 51 で、空振り防止の下限 60 を割った。**仕様が名指しで例に挙げた「工具の売り注文の延べ件数が母集団ごと減る」そのもの**
  - `TradePipelineTests.ToolOffersNeverDisappearOverSixtyDays`(seed:7 day=33 / seed:42 day=20)— 工具の売り注文が 0 件の日が出た。空振り防止の閾値ではなく**検出器の本体の断定**が破れている
  - `TradePipelineTests.OwnOutputIsNeverHoardedWhileTheHouseholdGoesWithout`(全5シード)— #174 の観測件数が固定値 180 から動いた。検出器が前提にしていた「自家供給できる6戸が6戸のまま続く」が④の付け替えで崩れた。**「母集団が縮んだ」は誤り**(フェーズ1 が実測し直した: seed1=168 / seed2=175 / seed3=169 / seed7=**189** / seed42=**188** で、**2シードは増えている**)。鍛冶や水車小屋番が自家供給できる職業へ入れば増える側にも動く
- 決めて報告した件数: **2 件**
  - テスト #13 の `P ≤ B ÷ 2` の作り方を「買い手に高めの相場観測を仕込む」側にした。仕様は2案を「どちらでもよい」としていたが、`StoreChoice.TrySelect` / `ErrandPlanner` が `EffectivePrice.Calculate` へ `trust: 0` を定数で渡しているため(信用の配線は [#44](https://github.com/stama72/visionary/issues/44) 未着手)、**信用割引の手は現状のコードでは機能しない**。理由はテストの remarks に書いた
  - テスト #13 の世界を `WorldDefinition.M0.Recipes` を流用した手組みで作った(`EconomySystemTestFixtures.BuildDefinition` はカスタムレシピを1つしか差し込めない)。`TradePipelineTests` の class doc コメントが謳う「世界は必ず `WorldGenerator.Generate` で作る」とは異なるが、仕様の「#13 だけは `FullPipeline(definition)` を通す」の範囲内

## 直さないと決めた指摘(1回目のフェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |

(レビューへ進まずに `IMPL-BLOCKED` で止まったため、なし)

## 巡ごとの件数(1回目のフェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |

(レビュアーを起動していない。`HEAD` のテストが赤のまま回しても、指摘が実装の欠陥か検出器の前提崩れかを切り分けられない)

## `mutator` の件数(1回目のフェーズ2)

- 当てた変異: **0 件** / 期待と食い違った数: —

**測っていない。** 仕様は「測るのはレビューの巡が閉じた後、コミット済みの `HEAD` に対して一度でよい」と定めており、レビューへ進んでいない。加えて `HEAD` のテストが赤なので、いま当てても C-1・C-2・C-3 の「期待 赤」が変異によるものか既存の 8 件の赤によるものか区別がつかない。**C-1・C-2・C-3 の実測は未消化のまま次のフェーズ2 へ持ち越す。**

---

# 2回目のフェーズ2(2026-09-23)

**フェーズ1 の裁定 D-A・D-B・D-C を当てて全緑にし、レビュー3巡と `mutator` の実測まで通した。**

## implementer の件数(2回目のフェーズ2)

- 止まって報告した件数: **0 件**(5回起動していずれも 0)
- 決めて報告した件数: **6 件**
  - D-C の空振り防止(day 0 の分布)の assert を、日次走査ループの**前**に置いた。仕様は配置順を指定していない
  - D-A の旧 remarks(R-1〜R-5。固定値 180 だった時点の変異実測)を**残し**、新しい下限(3 / 90)についての追記とした。仕様は D-A について旧 remarks の扱いを明示していなかった(D-C は明示していた)
  - **テスト #14 の段A は、「自職業を除く」行を消しても構造的に赤にならない。** 区画フィルタが破産世帯自身を含めて同区画の担い手を数えるので、候補が自職業のとき必ず自世帯がマッチして除外される。段A のブロックは契約の確認として残し、変異を捕まえるのは段B のブロックとした → **フェーズ2 のメインが仕様の別表を実測どおり狭めた**(`167d30a`)
  - #4(`OccupationChangesOnlyWhenAllThreeConditionsHold`)の置き直しを、`Theory` の `InlineData` に `Occupation` 列を足して **b 欠けの行だけ `Woodworker` にする**形で行った。世界の組み立てを共有する `Theory` の構造上、この行だけ被験者を変える最小の変更
  - `SelfSuppliableObservations` プロパティの doc コメント(「6戸 × 30日 = 180件」)も、指示が名指しした `ScannedSelfSuppliableEntryCount` と**同じ誤りの重複**として直した
  - コミット末尾の `Co-Authored-By` を、自セッションの attribution(Sonnet 5)にした

## 直さないと決めた指摘(2回目のフェーズ2)

| 巡 | 象限 | 指摘 | 直さない理由 |
| -- | ---- | ---- | ------------ |
| 2 | I-a | `TryFindLeastCarriedOccupation` の走査上限 `definition.OccupationCount` を定数 `5` / `Enum.GetValues<Occupation>().Length` に置き換える変異が**現状では等価**。仕様「添字・単位の約束」が名指しした「`Occupation` enum の要素数ではない」が、今のテスト集合では守られていない | **レシピ数が 5 でない `WorldDefinition` を作るテストが存在しない**(`EconomySystemTestFixtures.BuildDefinition` は常に5件)。塞ぐには定義の作り方そのものを足すことになり、本タスクのスコープを超える。**issue へ落とす** |
| 2 | I-a | `ArgumentNullException.ThrowIfNull` 5か所(`OccupationReassignment` 3 / `HouseholdSystem` 2)に検出器が無い | **リポジトリ全体で `ArgumentNullException` を断定するテストが1件も無い**既存の慣行である。本タスクだけ変えると、規約でないものを1箇所にだけ持ち込むことになる |
| 1 | II | 仕様 166 行目「**順1 より前に置いてもならない**(条件 c が前日の生産量を読むことになる)」を押さえる検出器が無い。#12・#13 はどちらも**順5 との前後関係だけ**を押さえており、`FullPipeline` で順3 を `ProductionSystem` より前へ動かしても落ちるテストが無い | 象限II。**issue へ落とす** |

## 巡ごとの件数(2回目のフェーズ2)

| 巡 | 守備範囲 | 象限I | 象限II | 疑い |
| -- | -------- | ----- | ------ | ---- |
| 1 | 全体(決定論規約・仕様との整合・「変えない既存コード」表・テストの判別力) | **2**(I-a 1 / I-b 1) | 1 | 0 |
| 2 | **網羅パス** — `OccupationReassignment` と `HouseholdSystem` の分岐・比較・定数・走査順を全列挙 | **5**(全て I-a。未被覆の判断点) | — | 0 |
| 3 | 網羅パスの境界の**外側**(置き直した検出器3本 / 配線の断定 #12・#13 / 「変えない既存コード」と現行 GDD-TDD / remarks の主張と事実) | **2**(全て I-a) | — | 0 |

**3巡で打ち切った**(上限は4)。**2巡目を網羅パスに充てたため、探索の巡は1巡目と3巡目の2つである**([01-review](../process/01-review.md)「網羅パス」— 巡は1つ進み、上限4は動かない)。3巡目の指摘2件はいずれも狭く、修正はテストの断定1本と doc コメントの訂正に収まった。**核心の境界は2巡目が列挙し切って塞ぎ終わっている**ため、4巡目を回しても減衰する。

**1巡目の象限I-b の訂正はフェーズ2 のメインが行った**(仕様の訂正は設計判断)。「手順1 と手順2 を1つのループに畳まない」の理由として書かれていた「世帯 Id 0 の付け替えが世帯 Id 1 の**担い手世帯数**に効く」が成立しない — 手順2 が他世帯から読むのは `Occupation` と `DistrictId` だけで、手順1 が書くのは `IsBankrupt` だけである。**分割を守っているのは GDD02b §4.1 の規則であって、テストではない。**

## `mutator` の件数(2回目のフェーズ2)

- 当てた変異: **4 件**(C-1 / C-2 / C-3 / **M-fold**)/ **期待と食い違った数: 0 件**

**M-fold は1巡目の象限I-b の訂正を裏付けるために足した変異である**(期待 **緑** = 1本も落ちない。実測も緑)。**「畳んでも結果が変わらない」を、赤ではなく緑の実測で確かめた**ことがこの1件の意味である。

**C-2 の追加測定**(仕様の裁定 D-C 末尾が要求): `EveryOccupationKeepsAtLeastOneCarrierOverSixtyDays` は C-2 で**赤**になった(seed 42 = day 23 `Miller` ほか / seed 7 = day 36 `Smith` ほか)。**受け入れ条件は引き続き #6 が落ちることであり、60日走行の赤は測定値として転記しただけである。**

## 3巡目が見つけたもの(フェーズ3 が PR 説明で拾う価値があるもの)

**裁定 D-A・D-B・D-C が固定値と日次下限を落とした根拠は「④が職業分布を動かす」の1点だが、置き直した3本はどれも④が1度も発火しない世界でも緑だった。** ④が到達不能になった日に残るのは**理由の消えた緩い検出器**である。`EveryOccupationKeepsAtLeastOneCarrierOverSixtyDays` に「60日のうち少なくとも1日、職業分布が day 0 と異なる」を足して塞いだ。**最初に分布が動いた日**(実測 2026-09-23): seed1=day 10 / seed2=day 17 / seed3=day 9 / seed7=day 11 / seed42=day 10。

あわせて、D-A の置き直しで**判別力が延べ 180 → 90 へ落ちた**ことを doc コメントに明記した。**自家供給3戸ぶん(日次3件)まで走査が縮む変異は、日次下限3・延べ下限90・day 0 の `SelfSuppliableHouseholdCount`・最終日の突き合わせループのどれも通り抜ける**(実測で確認済み)。構造的下限が 3 である以上これ以上は締められないが、「この件数が走査範囲の縮みを捕まえる」という旧い記述は残してはならない。

## フェーズ2 が止まった理由(`IMPL-BLOCKED`)

上の3件は、タスク仕様「3. パイプラインへの配線」の「赤の種類」表で**第3区分(検出器の前提が崩れた)**に当たる。同表は「**止まって報告する。前提の置き直しは設計判断であり、implementer もフェーズ2 のメインも決めない**」と定めている。第2区分(実測値・自然発生する(世帯, 日)・件数の閾値 → 値の追随でよい)ではない:

- `SmithNeverRunsOutOfToolsOverSixtyDays` は仕様が第3区分の**例として名指しした**ケースそのもの
- `ToolOffersNeverDisappearOverSixtyDays` が破れたのは空振り防止の下限ではなく**検出器が検出すると宣言した事象**(工具の売り注文が消えない)である。値を追随させることは検出器を無効化することと同じ
- `OwnOutputIsNeverHoardedWhileTheHouseholdGoesWithout` は母集団の件数が減っただけに見えるが、減った原因は**④の付け替えで世帯の職業が動いたこと**であり、検出器が母集団を「自家供給6戸 × 出力1品目 × 30日」と固定できた前提が崩れている

フェーズ1 が決めるべきこと(このまま渡す):

1. ④の付け替えが発火する世界で、この3検出器の母集団をどう置き直すか(職業を固定した母集団にするか、日ごとに担い手を数え直すか)
2. `ToolOffersNeverDisappearOverSixtyDays` の断定そのものを④の下で維持するのか。維持するなら、鍛冶が④で離脱しうる経済で「工具の売り手が消えない」を何が保証するのか([#196](https://github.com/stama72/visionary/issues/196) の④のゲート見直しと同じ土俵)
3. 上の判断が出るまで `HEAD` は赤である。実装本体(`HouseholdSystem` / `OccupationReassignment`)と新規テスト #1〜#13 は緑で、赤はこの3検出器の8インスタンスだけである

`HEAD` = `a38b5b2`。`dotnet build -c Release` 警告0 / `dotnet format --verify-no-changes --severity warn` 差分なし / `dotnet test -c Release` は **485 合格・8 失敗**。
