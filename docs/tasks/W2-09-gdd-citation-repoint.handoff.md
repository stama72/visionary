# W2-09 引き継ぎメモ(#112)

**フェーズをまたいで消えるものだけを持つ**([05-phase-sessions](../process/05-phase-sessions.md))。フェーズ3 が PR 説明へ転記して削除する。

## 却下した設計案(フェーズ1)

| 案 | 却下の理由 |
| -- | ---------- |
| **検出器を CI ジョブとして回す** | 開発者の判断。GDD の節番号を振り直した PR が、コード側の引用を直すまで赤になる。#101 が踏んだ経路を機械が塞ぐ効果はあるが、その代償を**いまは**取らない。**検出器は置くので、後から CI に載せる判断だけが残る** |
| **検出器の対象を `GDD02*` と `GDD06` に限る**(issue の閉じる条件の文言どおり) | 2026-09-20 の実測で、古い引用は `GDD02 §*` の 112 件だけ。`TDD01`(143 引用)・`GDD08`・`GDD06` には 1 件も無い。**全文書へ広げても結果が同じなので、広げるコストが 0 である。** 将来 TDD01 の節を振り直したときに拾える |
| **継続引用(`GDD02 §6.1・§9`)を無条件に直前の文書 Id へ引き継ぐ** | `GDD02d §4.3 の条件(a)〜(e)、#96 タスク仕様 §8` を `GDD02d §8` と誤検出した(`M0CalibrationTests.cs:9`)。**0 件を要求する検査では偽陽性のほうが有害である。** 引き継ぎを `タスク仕様` / `ADR-` / `#<数字>` で打ち切る規則に変えて 0 になった |
| **行をまたいで文書 Id を引き継ぐ**(`StateHasher.cs` の `§3.8` のような形を解決する) | 誤帰属の危険が、取りこぼし 35 件より大きい。**`UNRESOLVED` として件数だけ出す**ことにして、解決は放棄した。スクリプト冒頭の「残る穴」に書く |
| **表C(規則ごと古いコメント)の散文も本タスクで直す** | 直すと、規則を引き取る #98 / #38 / #39 の入力が消える。#112 が「式や規則は触らない」と決めている。**代償は、引用先だけが現行を指す中間状態がしばらく残ること** — 仕様の表C とフェーズ3 の issue コメントが、その間の唯一の目印である |

## 数えるもの(フェーズ2 が埋める)

**フェーズ2 は2セッションに分かれた。** 1回目は `IMPL-BLOCKED` で停止(検査スクリプトを実行できず、核心の確認 #2〜#5・#7 が未実施)。フェーズ1 が仕様を3点訂正し、無人フェーズにスクリプト実行を許して(`00f7ef5`)から2回目で完走した。

- implementer の件数(2セッション合計・4回起動): **止まって報告 1 件 / 決めて報告 9 件**
- 巡ごとの件数: **1巡目 1 件(象限 I-b)/ 2巡目(網羅パス)2 件(いずれも象限 I-b)。2巡で打ち切り**(理由は後述)

### 止まって報告 1 件(象限 I-b — 仕様の欠陥。1回目のセッション)

- **完了条件の `grep -rn 'GDD02 §9' src/ tests/` が 0 件にならない。** 表A が旧 `GDD02 §13.2` の行き先を `GDD02 §9.2` と定めているため、書き換え後に**正しい引用**が 2 件残り、grep が前方一致する。**フェーズ1 が `-E 'GDD02 §9([^.0-9]|$)'` へ訂正済み**(`00f7ef5`)

### 決めて報告 9 件

1回目のセッション(5件)のうち、**#1(継続引用の書式)と #4(`LC_ALL` を既定ロケールに委ねた)はフェーズ1 が仕様側で裁いた** — 前者は採用、後者はそれ自体が欠陥として確認 #8 になった(`00f7ef5`)。残りは実装細部:

1. 検出器から `bin/` `obj/` `.git/` を除外した(仕様は「拡張子で絞らない」としか言っていない)
2. 見出し収集ループで 2 語文字列を `set --` で空白分割する書き方を採った
3. `awk` へ渡すファイルリストが空のとき標準入力待ちで固まるのを避けるガードを足した(仕様に明記なし)

2回目のセッション(4件):

4. **ロケール非依存化の方式** — 仕様が許した2案のうち、`LC_ALL=C.UTF-8` 固定ではなく `length("§")` を実行時に測る形を採った。**理由: 固定案はそのロケールが無い環境(古い musl / 一部の macOS)で黙って元の誤報へ戻る。** 動的計算はロケールの種類も存在有無も前提にしない
5. **同じ根本原因を持つもう1箇所も直した** — `§` の直後が数字でない経路のフォールバック分岐。現状のコードベースに該当パターンは 0 件で確認 #8 の実測には現れないが、同一バグクラスを温存する理由がない
6. 分冊接尾辞の広げ方を `[a-d]?` → `[a-z]*` とした(仕様は「a〜d に限らない」としか言っておらず、文字クラスの選択は実装細部)
7. 冒頭コメントへ足す2項目の文の切り方・改行位置を既存4項目に合わせて整形した

### 直さないと決めた指摘

| 指摘 | 決定と理由 |
| ---- | ---------- |
| **`<文書Id> §<番号>` 以外の形の引用3件**(`GameDate.cs:14` / `Season.cs:10` のパス名指し、`M0CalibrationTests.cs:276` の `GDD` 接頭辞なし) | **書き換えない。** 表A・表B・表D の列挙外であり、直せば `UNRESOLVED 35` も動く。裸の継続引用36行を直さないと決めたのと同じ仕分け(ADR-0008)。**仕様の「残る穴」4つ目として記録し、フェーズ3 が issue へ落とす** |
| **`UNRESOLVED` を file:line 付きで出して監査可能にする**(1巡目が挙げた選択肢の1つ) | **採らない。** §「入出力の契約」が「集計はこの1行だけ」と定めており、**出力の契約の変更はフェーズ1 の判断**である。フェーズ3 が issue へ落とす |
| **枝番 `-N` を照合できるようにする**(2巡目 指摘1) | **検出器は変えない。** 契約が「`-4` は番号の一部にしない」と定めた設計どおりの挙動で、変えるのは契約の変更。**穴の5つ目として書くにとどめた。** 実在4件は現時点ではすべて正しい(実測) |
| **見出し収集をコードフェンス対応にする**(2巡目 指摘2) | **同上。** 実測で該当ゼロの潜在的な穴。**穴の6つ目として書くにとどめた** |
| **`docs/04-tdd/01-sim-core-and-m0.md:210` に旧番号 `§12-4` が残っている**(2巡目が発見) | **直さない。** 仕様が `docs/` を触らないと宣言しており、触れば機械が `SPEC-OUTSIDE` で止める。**CLAUDE.md「気付いたら直す」より凍結した仕様が優先する場面である。** フェーズ3 が issue へ落とす |

### 2巡で打ち切った理由

**2巡目に網羅パスを充てて境界を閉じたため**([01-review](../process/01-review.md)「網羅パス」)。1巡目の指摘(検出器が読めない引用の形がある)が「検出器の入力経路」という列挙可能な境界を露出させたので、2巡目で**全経路を列挙させ、各経路の偽陰性・偽陽性と「穴の一覧に含まれるか」を判定させた**。

結果、**「穴は4つで尽きているか」に「尽きていない、あと2つ」と確定した回答が出た**(枝番・見出し収集側)。あわせて、付け替え本体は1巡目が表A・表B・表D と1対1で、2巡目が `d45ac3f^` の旧節構成と全78件で突き合わせており、**独立に2回照合されている。** 3巡目の対象は6行の文書修正だけになり、減衰すると判断した。

**残る指摘は上の表のとおりすべてフェーズ3 の issue へ落とす。** 未処理のまま打ち切ったものは無い。

### フェーズ3 が落とす issue(このセッションで増えたぶん)

仕様の完了条件が持つ表C の 10 件に加えて、次の4件がある。

1. **`<文書Id> §<番号>` 以外の形の引用3件を検出器に載せる**(`GameDate.cs:14` / `Season.cs:10` / `M0CalibrationTests.cs:276`)
2. **`UNRESOLVED` 35 件を監査可能にする**(file:line を出すか否かは出力の契約の変更 → フェーズ1)
3. **枝番 `-N` と見出し収集側の限界**(穴の5つ目・6つ目)を検出器で塞ぐか、穴のまま運用するかの判断
4. **`docs/04-tdd/01-sim-core-and-m0.md:210` の旧番号 `§12-4` → `§8-4`**(`docs/` 配下にも旧番号が残っている実例。検出器の走査対象を `docs/` へ広げる判断とセット)
