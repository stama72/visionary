# ADR-XXXX: <決定を1行で。名詞句ではなく「何をどうする」>

| 項目 | 内容 |
| ---- | ---- |
| 状態 | 起案中 / 承認 / 棄却(ADR-XXXX で置き換え) |
| 日付 | YYYY-MM-DD |

<!--
起案の前に [README.md](README.md)「ADRか他文書かの切り分け」の3つの問いを通すこと。
  1. 却下した選択肢に価値があるか
  2. これは決定か、それとも予測か
  3. 読者は「なぜ」を知りたいのか、「今どうなっているか」を知りたいのか
3 が「今どうなっているか」なら、それは GDD / TDD / process の仕事であってADRではない。

**番号の予約**: `git worktree list` が自分の1本より多く返すときだけ、起案の時点で
README の表へ「起案中」の行だけを先に master へコミットする(README「運用ルール」)。
-->

## 背景

<!-- 何が起きて、なぜ今決める必要があるのか。事実と測定値で書く。決定を先に書かない。 -->

## 論点1: <決めること>

<!-- 論点が1つしか無いなら「## 検討した選択肢 / ## 決定 / ## 理由」を直に置き、論点見出しを作らない。 -->

> ステータス: 承認

<!--
多論点ADRは論点ごとにステータス行を持つ(README「運用ルール」)。
「1決定1ファイル」の「1決定」は**独立して覆せる単位**であり、覆されるのは論点ごとだからである。
後から読み替えが起きたら、この行に追記する:
  > ステータス: 承認 / WIP 上限は ADR-0006 論点4 で読み替え
-->

### 検討した選択肢

1. **<採る案>**
2. <却下する案>
3. <却下する案>

### 決定

**選択肢1。**

### 理由

- <なぜ選択肢1なのか>
- <**なぜ選択肢2・3を却下したのか**。ADRの価値はここにある。「他に選びようがなかった」としか書けないなら、そもそもADRではない>
- <**予測を書くならそう明示する。** 「これは仮説であり、◯◯で検証する」。検証の記録先も書く — 記録先の無い検証条件は、検証したことと誰も記録しなかったことが区別できない(ADR-0009 の実例)>

## 決定(まとめ)

<!-- 論点が2つ以上あるときだけ置く。表か箇条書きで、決まったことだけを並べる。 -->

## 帰結(引き受けるトレードオフ)

<!--
この決定を採ることで受け入れる不利益。ADR内で完結するものだけをここに書く。
「気をつける」で終わる行は帰結ではない。何が悪くなり、なぜそれを許容できるのかを書く。
-->

- **<何が悪くなるか>。** <なぜ許容できるか>

## 波及(反映先)

<!--
**この節を埋めて初めて決定が完了する。** 反映先が空なら節ごと省いてよい。
**反映先が決まらないものは、帰結ではなく issue に書く。** チェックできない行を並べると完了が判定できない。
-->

**この ADR は、以下を更新して初めて完了する。**

- [ ] <ファイル>(<何を変えるか>)
- [ ] CLAUDE.md(<規律として足すもの。無ければ行ごと消す>)
